# S15P11A406-186 모바일 좁은 폭 강건성 · 아이콘 일관성 · 랭킹/리액션 UI

> Jira: https://ssafy.atlassian.net/browse/S15P11A406-186
> Branch: `refactor/S15P11A406-186-mobile-text-robustness`
> 기준 브랜치: `develop` (`fb6d545`로 리베이스 — 음성 채팅 130 · 탁구 복원 187 · QA 182 병합 이후)
> 최종 갱신: 2026-08-04

실기기 관찰에서 시작해 네 갈래(A~D)를 다뤘고, 그 뒤 **실제 렌더 화면을 보며 7건을 더
고쳤다(E)**. 기준은 `npx ui-skills`의 `baseline-ui` · `fixing-accessibility` ·
`fixing-motion-performance`(skill로 등록돼 있지 않아 SKILL.md를 직접 읽어 적용).

원칙: 증상이 아니라 원인을 한 곳에서 고친다. 고쳤는지 확인할 수단을 먼저 만든다.

---

## ⚠️ 먼저 — 초기 진단 2건이 틀렸다

이 티켓 초반에 아래를 근본 원인으로 단정했는데 **둘 다 사실이 아니다.** 티켓 본문에도 정정을
남겼다. 옛 진단을 근거로 읽으면 안 된다.

| 틀린 주장 | 실제 |
|---|---|
| `break-keep` 사용 0건 — 한글 어절 보호가 없다 | `global.css`가 `:root`에 `word-break: keep-all` + `overflow-wrap: break-word`를 **이미 전역 적용** |
| `text-wrap: balance`가 `h3`에만 걸려 `h1`·`h2` 39곳이 무방비 | `h1, h2, h3` **전부**에 걸려 있다 |

원인은 두 가지다. `sed -n '28,50p'`로 `global.css`를 중간부터 읽어 26–27행의 `h1,` `h2,`를
놓쳤고, `break-keep`을 Tailwind **유틸리티만** grep해서 `@layer base`를 확인하지 않았다.

**교훈**: 「저장소에 X가 없다」를 유틸리티 grep만으로 결론 내리지 않는다. Tailwind 프로젝트는
같은 선언이 base 레이어에 있을 수 있다.

또 하나 — 계산된 스타일을 읽어 「셰브론이 회전하지 않는다」고 진단했는데 **이것도 틀렸다.**
브라우저 창이 표시되지 않으면 `document.visibilityState === 'hidden'`,
`document.timeline.currentTime === 0`이라 **애니메이션 타임라인이 진행하지 않는다.** 진행 중인
트랜지션의 시작값을 최종값으로 오해했다. 근거 없는 주석과 함께 넣은 코드는 되돌렸다.

**교훈**: 헤드리스/숨겨진 창에서 트랜지션·애니메이션에 의존하는 계산값은 신뢰할 수 없다.
최종값을 봐야 하면 `style.transitionProperty = 'none'`으로 끄고 읽는다. 정적 스타일(색·크기·
stroke)은 유효하다.

---

## A. 좁은 폭 강건성

### [x] A-1. 320px 검증 수단 확보

**현재 상태.** `global.css`가 `html`·`body`에 `min-width: 320px`으로 지원 하한을 선언했는데
`playwright.config.ts`의 모바일 프로젝트는 Pixel 7(412px)·iPhone 15(393px) 둘뿐이다.
**가장 잘 깨지는 폭이 검증 밖**이라 코드가 아니라 실기기에서 먼저 발견됐다.

**어떻게.** `mobile-320` 프로젝트(320×568)와 `e2e/mock/narrow-width.spec.ts` 신설.

```bash
npx playwright test --project=mobile-320 narrow-width
```

**스크린샷이 아니라 기하로 판정한다.** 스크린샷 비교는 폰트 렌더 차이로 흔들리고 무엇이
틀렸는지 말해 주지 않는다. 가로 넘침은 숫자로 판정되고 넘친 요소의 이름까지 짚을 수 있다.

검사 3종:

- `expectNoHorizontalOverflow` — 감춰 주는 조상이 없는데도 뷰포트를 넘는 요소
- `expectKoreanWordBreakProtected` — 전역 `keep-all`이 실제 텍스트까지 닿는지
- `labelFit` — 짧은 라벨이 **제 칸 안에 한 줄로** 들어가는지

**손대는 곳**: `playwright.config.ts`, `e2e/mock/narrow-width.spec.ts`(신설)

#### 이 스펙을 손댈 때 지킬 것

세 가지를 의식적으로 넣었으니 지워지지 않게 한다.

1. **헛도는 단정을 만들지 않는다.** 한글 검사에 `examined > 0` 가드가 있다. 대상이 0개면
   통과가 안전을 뜻하지 않으므로 그 사실 자체를 실패로 만든다.
2. **단정이 실제로 회귀를 잡는지 확인한다.** 태그라인·라운드 라벨 단정은 수정을 떼어 실패하는
   것을 확인했다. `labelFit`은 처음에 줄 수만 셌는데 **수정을 떼도 통과했다** — `nowrap`만
   걸어도 줄 수는 1이 되고, 대신 라벨이 칸을 삐져나와 옆 버튼과 겹치며 그 겹침은 뷰포트
   안이라 넘침 검사에도 안 걸린다. `scrollWidth`와 부모 경계를 함께 봐야 잡힌다.
3. **덮지 못한 것은 이유를 적어 비워 둔다.** 통과하는 스펙으로 위장하지 않는다.

### [x] A-2. 나란히 선 요소가 텍스트를 압착해 줄이 두 배로 늘던 것

**현재 상태.** 브라우저에서 393px과 320px의 렌더된 **줄 수**를 비교해 찾았다. 원인은 줄바꿈
속성이 아니라 flex 행에서 `shrink-0` 형제가 텍스트 칸을 압착하는 것이다.

| 요소 | 받는 폭 | 결과 |
|---|---|---|
| 랜딩 태그라인 `h1` | 320 − 40(padding) − 12(gap) − **116(초대 코드 칩 `shrink-0`)** = **152px** | 24px 글자로 **4줄 · 120px** |
| 캐러셀 스와이프 힌트 | 280 − 55(카운터) − 102(tablist) − 24(gap) = **99px** | **2줄 · 36px** |

태그라인은 세로 한 층을 아끼려고 초대 코드 옆에 붙인 것인데, **그 층보다 큰 높이를
태그라인에서 되돌려 받고 있었다.**

**어떻게.** `--breakpoint-tiny: 22.5rem`(360px)을 토큰에 두고 **하한 전용** `max-tiny:`로 쓴다.

- 태그라인: 360px 미만에서 쌓아 280px을 받게 → 4줄 → **2줄**(120 → 60px). 행 높이도
  131 → **125px로 오히려 감소**
- 힌트: 그 폭에서 감춘다 — 오른쪽 정렬 2줄은 안내가 아니라 깨진 것으로 읽힌다. 스와이프는
  카드로 발견되고 위치는 tablist가 계속 말한다
- 360px 이상 배치는 그대로(393px에서 `flex-direction: row` · h1 2줄 유지 확인)

**손대는 곳**: `src/styles/tokens.css`, `src/landing/screens/EntryPage.tsx`,
`src/landing/components/LandingProgress.tsx`

### [x] A-3. 게임 헤더 라운드 라벨이 접히던 것

**현재 상태.** 320px 헤더는 고정 요소가 폭을 다 먹는다 — 나가기 44 + 도움말 44 + 소리 44 +
타이머 52 + 좌우 여백 32 + gap 60 = **276px**. `flex-1`인 턴 표시 칸에 **56px**만 남고
「Round 01 / 12」는 넓은 자간까지 합쳐 약 110px이라 두 줄로 접혔다.

**어떻게.** 그 폭에서는 `Round`와 넓은 자간을 뺀다. 숫자 쌍만 남아도 옆의 원형 타이머와
나란히 놓이면 라운드 진행으로 읽히고, 정확한 낭독은 sr-only `h1`이 이미 한다
(「요르 게임 진행 중 · N / 12 라운드」). `whitespace-nowrap`도 함께 걸어 폭이 더 줄어도
접히는 대신 잘리게 한다 — 접히면 헤더 안에서 줄 수가 흔들린다.

**정상 리플로는 건드리지 않았다.** 닉네임 제목이 2줄, 튜토리얼 안내문이 4줄이 되는 것은
좁아진 폭에 맞춰 접히는 것이고 `text-balance`가 이미 다룬다.

**손대는 곳**: `src/yacht/screens/GamePlayHeader.tsx`

### [x] A-4. 게임 화면 검증 공백 해소 (→ E에서 실전 흐름까지 확장했다)

**현재 상태.** 게임 화면은 좁은 폭에서 가장 붐비는데 스펙이 닿지 않았다.
`startHostedGame`이 찾는 `게임 시작` 버튼이 mock 흐름에서 `게임 시작 · 호스트 전용`이 된다
(`waitingSnapshot`에 `hostId`가 없어 `isHost`가 거짓). **같은 이유로 `game-flow.spec.ts`
4건이 이 티켓 전부터 이미 실패**하며 **S15P11A406-167 「e2e 스펙 복구」**가 그 건이다.

**어떻게.** `/tutorial`이 같은 `GamePlay`·`GamePlayHeader`를 쓴다 — 실시간 방 없이 같은
레이아웃을 볼 수 있어 `test.fixme`를 실제 스펙으로 바꿨다. 실전 전용 요소는 덮이지 않으므로
한계를 주석에 적었다(→ C-1).

**손대는 곳**: `e2e/mock/narrow-width.spec.ts`

---

## B. 아이콘 일관성

### [x] B-1. 아이콘 역할 글리프 → 공용 SVG (12곳)

**현재 상태.** `🔇`·`✕`·`✓`·`‹`·`?`·`!`·`▼`·`⋯`가 문자로 그려져 있었다. 문자는 세 가지를
못 한다 — `currentColor`를 따르지 않아 이모지는 무조건 플랫폼 색으로 나오고(모노톤 크롬에서
혼자 튄다), 폭과 베이스라인이 기기마다 달라 같은 줄의 줄바꿈 지점까지 흔들리고, 글자라서
`size-*`로 크기를 통제할 수 없다.

**어떻게.** `shared/components/Icon.tsx` 신설. **`CategoryIcon`의 규약을 그대로 가져온다** —
20×20 `viewBox`, `currentColor`, `aria-hidden` 고정, 크기는 호출부 `className`. 저장소에
아이콘 방식이 이미 하나 있으므로 두 번째 방식을 만들지 않았고, 아이콘 라이브러리도 달지 않았다
(교체 대상 12개에 번들을 늘릴 이유가 없다).

| 아이콘 | 교체 대상 |
|---|---|
| `IconSound` | 🔇/🔊 — `EntryPage` · `GamePlayHeader`. `muted` 분기를 아이콘 안으로 들여 호출부 삼항 제거 |
| `IconClose` | ✕ — `GamePlayHeader` · `LandingRoomCodePanel` |
| `IconCheck` | ✓ — `GamePlay` · `ScoreRow` · `InAppBrowserGate` |
| `IconBack` | ‹ — `InvalidInvitePage` · `NicknamePage` |
| `IconWarning` | ! — `InAppBrowserGate` · `InvalidInvitePage` |
| `IconHelp` | ? — `GamePlayHeader` |
| `IconChevron` · `IconEllipsis` | ▼ · ⋯ — `RankingTicker` |

**지시 범위를 넘긴 판단 1건**: 원래 목록은 9곳이었는데 `?`·`!` 3개를 더했다. `✕`만 SVG로
바꾸고 옆의 `?`를 문자로 두면 같은 버튼 줄에서 "일관된"이 깨진다.

`ScoreRow`의 `' ✓ 선택'`은 `aria-label`에 섞여 있어 접근성 이름이 `Fours 12 선택`으로 바뀐다
— `✓`는 낭독에서 "체크 표시"로 읽히는 노이즈였다. 해당 테스트도 고쳤다.

**모양은 나중에(E-8) 눈으로 보고 3개를 고쳤다** — 경고는 삼각형을 얻었고, 점 세 개와
음소거 ✕는 작은 크기에서 무너지던 것을 바로잡았다.

**확인.** 렌더된 SVG를 실측해 세 아이콘이 각자 자리의 텍스트 색을 상속하는 것을 확인했다 —
이모지로는 불가능했던 부분이다.

| 아이콘 | 크기 | stroke 실측 | 상속 토큰 |
|---|---|---|---|
| 랜딩 사운드 | 18×18 | `rgb(164,165,170)` | `landing-text-muted` |
| 뒤로 가기 | 18×18 | `rgb(247,247,245)` | `content` |
| 경고 | 28×28 | `rgb(255,107,102)` | `danger` |

**손대는 곳**: `src/shared/components/Icon.tsx`(신설) + 위 표의 8개 파일

### [x] B-2. 표정 이모지는 유지, 카피 장식은 삭제

사용자 지시로 세 계열을 갈랐다.

- **계열 B 유지** — `ReactionDock`의 👍😂😱👏🫡와 fallback 💬. **아이콘이 아니라 표정이다.**
  단색 평면 SVG로 다섯 감정을 서로 구별되게 만들 수 있는지는 그려 봐야 아는 문제고, 웃음과
  놀람을 색 없이 가를 수 없다면 이모지를 남기는 것도 정답이다(→ D-2).
- **계열 C 삭제** — `'스매시! 💥'` → `'스매시!'`. 아이콘이 아니라 토스트 카피 속 장식이고,
  나머지 9개 라벨에 이모지가 없어 형제와 맞다.
- `app/dev/`의 `↑`·`←`·`→`는 개발 전용이라 범위 밖.

**손대는 곳**: `src/pingpong/PingPongGame.tsx`

---

## C. 주간 랭킹 띠

### [x] C-1. 글로우·`will-change`·일시정지·빈 상태

**글로우 2개 제거.** `LandingProgress`가 이미 같은 판단을 적어 뒀다 — "화면에서 유일하게
빛나는 레드는 CTA여야 한다". 랭킹 띠는 랜딩 최상단이라 글로우를 두면 그 아래 CTA와 시선을
다툰다. 1위 배지는 배경색 반전으로 이미 구분되고, 라벨 점은 맥동(`ring-pulse`)이 살아 있음을
이미 말한다. 계산된 `box-shadow`가 둘 다 `none`인 것을 확인했다.

**`will-change`를 활성 애니메이션으로 좁힘.** `motion-reduce`에서는 애니메이션이 없는데
`will-change-transform`이 남아 레이어를 붙잡고 있었다 → `motion-safe:`로 좁힌다.
(`baseline-ui`: will-change는 활성 애니메이션 밖에서 쓰지 않는다)

**키보드·터치 일시정지.** `hover`만 있어 마우스 없는 환경에서는 흐르는 띠를 멈출 방법이
없었다 → `focus-within` 추가. (`fixing-accessibility`: hover 전용 인터랙션은 키보드 등가물
필요)

**빈 상태가 행동을 잃던 것.** narrow 뷰포트가 ~168px뿐이라 기존 문구("이번 주 기록이 아직
없어요 — 로그인하고 첫 순위의 주인이 되어보세요")가 `truncate`로 앞머리만 남고 **행동이
통째로 잘렸다.** 빈 상태에는 다음 행동이 하나 남아 있어야 하므로 "로그인하고 1위 도전하기"로
줄였다 — 기록이 없다는 사실은 이름이 하나도 없는 것으로 읽히고, 무엇의 순위인지는 왼쪽 고정
라벨이 말한다.

**손대는 곳**: `src/landing/components/RankingTicker.tsx`,
`src/landing/components/__tests__/RankingTicker.test.tsx`

---

## D. 이모지 리액션

### [x] D-1. 입력 — Tab으로 도달할 수 없었다

**현재 상태.** 픽커는 닫혔을 때 트리거를 아래로 밀지 않으려고 `absolute`로 띄웠고, 그 결과
DOM에서 트리거보다 **앞**에 있다. 열어도 Tab을 앞으로 눌러서는 칸에 닿을 수 없었다
(`fixing-accessibility`: 모든 인터랙티브 요소는 Tab으로 도달 가능해야 한다).

**어떻게.**

- 열면 첫 칸에 포커스. 그 뒤는 방향키가 옮긴다(`role="toolbar"`, 양 끝 wrap)
- 열려 있어도 Tab에 걸리는 칸은 현재 하나뿐(roving tabindex) — 다섯 칸이 각각 Tab을 먹으면
  헤더까지 다섯 번을 더 눌러야 한다
- Escape로 닫을 때 트리거로 포커스 복귀 — 사라진 요소에 포커스가 남으면 다음 Tab이 문서
  처음으로 튄다
- 바깥 누르면 닫힘 — 랜딩 랭킹 드롭다운과 같은 규칙(모달이 아니므로 뒤를 잠그지 않는다)
- **보낸 뒤에 닫지 않는다** — 리액션은 대화가 아니라 환호라서 연달아 누르는 것이 기본인데,
  매번 닫혀 세 번 보내려면 세 번 열어야 했다. "게임 화면을 계속 덮지 않는다"는 원래 의도는
  이제 바깥 누르기·Escape·트리거 재클릭이 받는다 — 그 전에는 자동 닫힘이 **유일한 수단**이라
  닫아야 했다

방향키 헬퍼는 `landing/tablistNavigation.ts` → **`shared/rovingFocus.ts`로 이동**
(`resolveTablistKey` → `resolveRovingKey`). 의존 방향이 `landing → room → yacht`라 야추가
랜딩을 import할 수 없고, 두 도메인이 함께 쓰는 순수 함수는 경계 모듈 `shared`의 몫이다.
새 구현을 만들지 않았다.

**손대는 곳**: `src/shared/rovingFocus.ts`(이동), `src/shared/__tests__/rovingFocus.test.ts`,
`src/landing/components/LandingProgress.tsx`, `src/yacht/components/ReactionDock.tsx`,
`src/yacht/components/__tests__/ReactionDock.test.tsx`

### [x] D-2. 표시 — 동시 도착이 서로를 가렸다

**현재 상태.** 좌우(`--drift`)만 흔들어 같은 순간에 온 것들이 **같은 높이에 한 줄로**
올라갔고, `motion-reduce`에서는 제자리에 뜨므로 닉네임 필이 2.2초 동안 그대로 겹쳤다.
**파일 주석이 이미 알고 있던 결함**이고 "실제로 겹쳐서 못 읽겠다는 얘기가 나오면 그때 세로
오프셋을 추가한다"고 미뤄 둔 것이다.

**어떻게.** `--lift`로 도달 높이까지 항목마다 흔든다. keyframe은 `-8.5rem`에 `--lift`를
더하고, `motion-reduce`에서는 호출부의 `translate-y`가 그대로 적용돼 같은 값으로 세로
분산된다.

`LIFTS` 길이(3)를 `DRIFTS`(5)와 **서로소**로 뒀다. 같은 길이면 두 값이 같은 주기로 돌아
조합이 5가지뿐인데, 서로소면 15가지가 돌아간다 — `MAX_FLYING`(12)보다 크므로 **화면에 함께
떠 있는 것들끼리는 절대 같은 좌표를 쓰지 않는다.**

**손대는 곳**: `src/styles/tokens.css`(`@keyframes reaction-float`),
`src/yacht/components/ReactionDock.tsx`

---

---

## E. 실전 화면까지 320px 실물로 확인하고 고친 것

여기까지가 「E-1 시각 검토」·「E-2 미확인 화면」을 실제로 한 결과다. **아래 7건은 전부 눈으로
본 뒤 좌표로 확인했다** — 이번에 처음으로 실제 렌더 화면을 봤다.

### 어떻게 봤나 — 헤드리스 브라우저로 실물 스크린샷

작업 환경의 브라우저 창은 여전히 프레임을 그리지 않는다(`visibilityState === 'hidden'`).
대신 **Playwright의 headless Chromium은 정상적으로 합성한다** — e2e 하네스를 그대로 쓰고
`page.screenshot()`만 얹어 320×568과 412×915을 같은 흐름으로 찍어 나란히 비교했다.
(수집용 스펙은 커밋하지 않았다. 필요하면 `e2e/mock`에 임시 스펙 하나로 되살리면 된다.)

**실전 게임·결과 화면이 처음으로 열렸다.** 막고 있던 것은 `waitingSnapshot`에 `hostId`가
없어서 mock 흐름에서 호스트가 되지 않는 것 하나였다(→ E-4의 결정 기록).

### [x] E-1. 연결 배너가 헤더를 읽을 수 없게 덮었다 (모든 폭)

**320px만의 문제가 아니었다.** 배너는 3D 트레이 리사이즈를 피하려고 헤더 위에 오버레이로
띄우는데(의도된 배치), 배경이 상태 색 **틴트**(`bg-warning/12`)뿐이라 아래 헤더 글자가 그대로
배어 나왔다 — 배너 두 줄과 헤더 세 줄이 겹쳐 **양쪽 다 못 읽는다.** 412px에서도 같다.

**어떻게.** 덮을 것이라면 불투명해야 한다. 배너 뒤에 불투명한 판을 한 장 깔고,
`has-[p]:`로 **문구가 있을 때만** 판이 생기게 했다 — 배너는 live region 유지를 위해 항상
마운트되고 문구가 있을 때만 `<p>`를 그리므로, 그 조건을 그대로 읽는다.

**손대는 곳**: `src/yacht/screens/GamePlay.tsx`

### [x] E-2. 헤더 턴 라벨이 어절 중간에서 잘렸다 (320px)

「내 턴이에요」가 **「내 턴이」로 끊겨** 오작동처럼 읽혔다. 원인 둘:

1. `truncate`가 **flex 컨테이너**에 걸려 있었다 — `text-overflow`가 익명 플렉스 아이템에
   닿지 않아 말줄임조차 붙지 않는다. 글자를 가진 요소로 내렸다.
2. 그래도 이 칸은 56px뿐이라 말줄임을 붙여도 한 글자만 남는다 → **A-3과 같은 처방**으로
   그 폭에서만 짧은 라벨(`내 턴` · `제출 완료` · 닉네임)로 갈랐다.

**손대는 곳**: `src/yacht/screens/GamePlayHeader.tsx`

### [x] E-3. 트레이 라벨이 칩 위에 겹쳐 그려졌다 (320px)

`롤링 존 · 나 · 굴림 1/3`(좌상단)과 `흔들기`·`3회 남음`·`ⓘ`(우상단)는 서로를 밀 수 없는
절대 배치 형제다. 폭 합이 약 300px이라 320px에서 글자가 칩 위로 올라갔다.
→ 그 폭에서 감춘다(A-2 캐러셀 힌트와 같은 판단). 굴림 횟수는 칩이, 턴은 헤더가 이미 말한다.

**손대는 곳**: `src/yacht/screens/GameDiceTray.tsx`

### [x] E-4. 2D 대체 주사위가 트레이 상·하단 띠를 덮었다 (320px · motion-reduce)

`prefers-reduced-motion`이면 2D 대체 화면이 트레이를 `inset-0`으로 덮는다. `size-14` 다섯
개는 320px에 한 줄로 들어가지 않아 두 줄로 접히고, 그만큼 자라 **굴림 횟수 칩과 킵 레일
라벨 위로 겹쳤다.** → 그 폭에서 `size-11`(탭 타깃 44px 하한은 지킨다) + 여백 조정으로 한 줄에
넣고, 위쪽 띠 높이만큼 패딩을 얹었다.

**손대는 곳**: `src/yacht/components/PhysicsDiceFallback.tsx`

### [x] E-5. 떠오른 리액션이 화면 오른쪽으로 빠져나갔다 (320px)

**D-2에서 고친 그 자리다.** 독은 화면 오른쪽 끝에 붙어 있는데 `DRIFTS`에 양수가 있어
이모지와 닉네임 필이 뷰포트 밖으로 밀렸고, 필(`max-w-24`)이 버튼(`w-tap`)보다 넓은데 가운데
정렬이라 drift가 0일 때도 26px 삐져나갔다 — **누가 보냈는지가 잘려 안 읽힌다.**

→ `DRIFTS`를 **0 이하로만** 두고(왼쪽은 트레이 안쪽이라 얼마든지 흩어져도 된다),
`items-center` → `items-end`로 오른쪽 끝을 맞췄다. `LIFTS`(3)와 서로소인 길이 5는 그대로다.

**넘침 검사로는 잡히지 않는다** — `GamePlay`의 main이 `overflow-hidden`이라 그 안의 넘침은
전부 "조상이 감춰 준다"로 분류된다. 좌표로 직접 재는 단정을 새로 넣었다.

**손대는 곳**: `src/yacht/components/ReactionDock.tsx`

### [x] E-6. 결과 화면 순위와 대기실 참가자 목록이 짜부라졌다 (320×568)

**이 화면들의 본문이 안 보였다.** 320px 기기는 높이도 568px이라 고정 요소가 프레임을 다 먹고,
`flex-1`인 스크롤 목록이 남은 것만 받는다 — 결과 화면 순위 **23px**(한 줄도 안 보인다),
대기실 참가자 목록 **4px**. `toBeVisible`로는 잡히지 않는다(잘린 행도 visible이다).

**어떻게.** 목록에 하한(`min-h-28`)을 주고, 프레임을 `overflow-hidden` → `overflow-x-hidden`
으로 바꿨다(세로는 `auto`로 계산된다) — 내용이 넘치면 **프레임 안에서** 스크롤된다.
가로는 그대로 감춘다(결과 화면 레드 글로우가 26rem이라 320px에서 좌우로 삐져나온다).

**`min-h-svh`로 페이지 자체를 늘리는 쪽을 먼저 시도했고 되돌렸다 — 렌더러가 죽었다**(→ 결정 기록).

**손대는 곳**: `src/yacht/screens/GameResult.tsx`, `src/room/screens/LobbyPage.tsx`,
`src/yacht/components/ResultRanking.tsx`(목록에 접근성 이름 부여)

### [x] E-8. 아이콘 모양 판단 — 3개를 고쳤다

실제 사용 크기(12·14·16·18·28px)를 44px 버튼 안에 나란히 놓고 96px 확대와 함께 한 장으로
찍어 봤다. `strokeWidth`는 **1.8을 유지한다** — 2.0과 나란히 놓고 봤지만 실제 크기에서
구별되지 않았고, 굵힐 이유가 없다. 대신 <b>작은 크기에서 형태가 무너지는 3개</b>를 고쳤다.

| 아이콘 | 무엇이 문제였나 | 어떻게 |
|---|---|---|
| `IconWarning` | 느낌표만 있어 12~16px에서 **세로 막대 하나**로 보였다 — 경고가 아니라 얼룩이나 `l`로 읽힌다 | 삼각형 테두리를 함께 그린다. 글자가 뭉개지는 크기에서도 모양만으로 경고를 말하고, `InAppBrowserGate` 체크리스트에서 `IconCheck`와 실루엣으로 갈린다 |
| `IconEllipsis` | `d="M10 10h.01"`(길이 0 선분 + 둥근 끝) 방식이라 점 지름이 곧 `strokeWidth`다 — 16px에서 **1.4px**로 사실상 사라졌다 | 점을 칠한 원(`<circle r>`)으로 바꿨다. 반지름이 viewBox 기준이라 크기와 함께 자란다. `IconWarning`·`IconHelp`의 점도 같이 바꿨다 |
| `IconSound`(muted) | ✕가 스피커 몸통에 붙어 14px 이하에서 한 덩어리로 뭉개졌다 — "켜짐"과 실루엣이 구별되지 않는다 | ✕를 한 칸 떼고 조금 작게 |

나머지 6개(체크·닫기·뒤로·소리 켜짐·셰브론·도움말)는 모든 크기에서 읽힌다 — 그대로 둔다.
스와치와 별개로 실제 화면(초대 오류 28px 경고, 헤더 18px 3종)에서도 확인했다.

### [x] E-7. `whitespace-nowrap` 13곳 재검토 — 바꿀 것이 없었다

각각 "줄바꿈보다 넘치는 편이 나은 곳"인지 확인했다. 결론: **전부 타당하다.**

| 분류 | 위치 |
|---|---|
| 짧은 라벨·배지 | `RollCounter` · `RankingTicker` 라벨 · `LandingHeroCard` 메타 필 · `EntryPage` 워드마크 · `GamePlayHeader` 라운드/선두 |
| 넓은 레이아웃 전용(그 폭에서 한 줄에 들어감) | `GamePlay` 점수표 제목 · `GameDiceTray` 안내문 · `PartyDashboardPage` 참가자 · `EntryPage` 태그라인(`desktop:not-sr-only`) |
| 흘러가도록 의도한 트랙(조상이 감춘다) | `RankingTicker` 항목 |
| `truncate`·자동 축소와 짝지어진 것 | `ReactionDock` 닉네임 필(`max-w-24 truncate`) · `RollResultCallout`(폭을 재서 폰트를 줄인다) |

---

---

## G. develop 리베이스 뒤 정리 (음성 채팅 · 탁구 복원 · QA 182)

`develop`이 14커밋 앞서 있어 리베이스했다(충돌 2건: `GamePlayHeader`의 소리 버튼,
`PhysicsDiceFallback`의 aria-label — 둘 다 develop의 새 기능을 살리고 이 티켓의 수정을 얹었다).
그 뒤 **이 티켓이 세운 규약과 어긋난 것 세 갈래**를 정리했다.

### [x] G-1. 첫 진입 안내 두 개가 겹쳐 코치마크를 닫을 수 없었다

**두 티켓이 각자 옳게 고친 것이 만나 막혔다.** 182가 모션 안내를 "켤 수 있는 즉시" 자동으로
열도록 바꿨는데(그전에는 아무도 칩을 누르지 않았다), 143의 첫 진입 코치마크와 같은 순간에 뜬다 —
모션 안내가 `z-30`이라 코치마크(`z-6`)의 **「알겠어요」를 덮어** 첫 진입 사용자가 코치마크를
닫을 수 없다. Playwright가 "element intercepts pointer events"로 잡아 준 실제 차단이다.

→ **한 번에 하나씩 띄운다.** 코치마크가 떠 있으면 모션 안내를 미루고, 코치마크를 닫는 순간
이어서 띄운다. 두 티켓의 의도(자동으로 알린다 · 첫 진입에 툴팁을 가리킨다)가 모두 유지된다.
182가 남긴 단정 3건은 "닫으면 이어서 뜬다"로 고쳤다.

**손대는 곳**: `src/yacht/screens/GameDiceTray.tsx`,
`src/room/screens/__tests__/GamePage.test.tsx`, `src/yacht/screens/__tests__/GamePlay.motion.test.tsx`

### [x] G-2. 새로 들어온 UI가 다시 문자 글리프를 쓰고 있었다

B-1이 "UI 역할 글리프는 공용 SVG"로 정리한 자리에, 음성 채팅과 탁구 복원이 이모지·문자를
일곱 군데 새로 들여왔다. 같은 규약으로 맞췄다.

| 글리프 | 있던 곳 | 바꾼 것 |
|---|---|---|
| `🎙️` | 게임 헤더 배지 · 대기실 오디오 버튼 · `PeerMicButton` · 오디오 시트 | **`IconMic` 신설**(4곳) |
| `🎵` · `🎲` | 오디오 시트 볼륨 줄 | `IconMusic` 신설 · `IconSound` 재사용 |
| `×` | `Modal` · `MotionPermissionPanel` 닫기 | `IconClose`(이미 있던 것) |
| `‹` | 탁구 「게임 선택」 뒤로가기 | `IconBack`(이미 있던 것) |

마이크 배지는 **초록**으로 둔다 — 회색 소리 아이콘 위에 같은 색으로 얹으면 아이콘의 일부로
읽힌다. 게임 헤더와 대기실이 같은 조합(소리 + 통화 중 배지)을 쓴다.

**남긴 것**: 탁구의 `🧩`·`🏆`(3D 실패·승리 연출 — 아이콘이 아니라 장식이고 `text-5xl`이다),
`◀ P1`·`P2 ▶`(코트 안 방향 표시), 트레이의 `📱`(흔들기 칩) — B-2의 계열 판단과 같다.

### [x] G-4. 연습 모드 입구를 플레이 버튼 위로 (사용자 요청)

**현재 상태.** 연습 입구는 진행 표시줄 **아래 층**에 있었다. 연습이 있는 게임(야추)에서만
그 층이 생겼다 사라져 **게임을 넘길 때마다 페이지 레이아웃이 흔들렸고**, 좁은 화면에서는
그만큼 히어로 카드가 눌렸다(`hasFooter`가 `game.live`까지 세고 있었다).

**어떻게.** 카드 안 하단 띠로 옮겨 **플레이 버튼 바로 위**에 세운다. 그 띠는 아래가 고정
(bottom 앵커)이라 한 줄이 늘어도 플레이 버튼은 제자리다. 자리는 **모든 카드가 함께 비워
둔다** — 카드마다 띠 높이가 다르면 캐러셀이 미끄러질 때 흔들려 보인다(`HeroCta` 주석).

**확인(실측).** 플레이 CTA의 y좌표가 야추·탁구·라이어스에서 모두 같다 — 320px에서 425,
데스크톱에서 496. 예전에는 야추에서만 아래 층이 생겨 그만큼 달랐다.

**손대는 곳**: `src/landing/components/LandingHeroCard.tsx`,
`src/landing/components/LandingHeroCarousel.tsx`, `src/landing/screens/EntryPage.tsx`

### [x] G-3. 죽은 `VoiceButton` 삭제

오디오 시트로 입구가 합쳐진 뒤 아무도 import하지 않는데 남아 있었다(주석은 옛 설계를 설명한다).
`VoiceContext`의 주석도 지금 UI(오디오 시트 · `PeerMicButton`)를 가리키게 고쳤다.

### 확인한 것

- 320px에서 **오디오 시트**(대기실·게임), **탁구 로컬 게임**, 새 아이콘 3종을 스크린샷으로 봤다 —
  넘침·겹침 없음. `IconMic`·`IconMusic`도 12~28px 스와치로 확인했다.
- **마이크가 켜진 상태의 배지는 실물로 보지 못했다** — headless에서 `getUserMedia`를 통과시키려면
  브라우저 실행 인자를 손대야 한다. 12px에서 모양이 읽히는 것은 스와치로만 확인했다.

---

## 검증 결과

리베이스(`fb6d545`) 뒤 기준이다.

`npm run check`(경고 2 — 둘 다 기존 `scripts/import-hand-voice.mjs`) · `npm run typecheck` 통과 ·
**단위 테스트 937개 전부 통과** · `npm run build` 성공.

**e2e(mock) — 네 프로젝트 205 통과 / 1 실패**(`npx playwright test --workers=1`, 7분).
남은 1건은 `mobile-safari`의 `participant-view` "watches the active player roll"이고
**이 브랜치와 무관한 flake**다 — 같은 명령을 이 브랜치 변경 없이 돌려도 4회 중 1회 실패한다
(2D 대체 주사위가 그려지지 않아 `6 주사위 KEEP`이 0개). WebKit에서만 난다.

리베이스 전 기준으로는 **네 프로젝트 206건 전부 통과**였다. 이 브랜치 시작 시점에는
`mobile-320` 11건 · 각 chrome 프로젝트 1건 · `mobile-safari` 1건이 실패하고 있었고, 셋 다
원인이 달랐다(→ 아래 「E. e2e 되살리기」).

> **병렬(`--workers=2` 이상)로 돌리면 이 기기에서 결과가 흔들린다** — 같은 스펙이 단독으로는
> 통과하고 병렬에서는 heading을 못 찾는다. 프리뷰 서버 하나에 워커가 몰릴 때의 문제로,
> 스펙 내용과 무관하다. 실패 목록을 볼 때는 `--workers=1`로 한 번 더 확인할 것.

### E. e2e 되살리기 — 세 가지 원인

**1. `mobile-320` 프로젝트가 "사파리 UA를 쓴 크로미움"이었다** (`invalid-invite` 2 · `smoke` 1).
`devices['iPhone SE']`는 `defaultBrowserType: 'webkit'`인 프로필인데, 이 프로젝트는 브라우저를
지정하지 않아 크로미움으로 돈다. 그 조합에서 화면 전환(view transition)이 겹치면
`AbortError: Old view transition aborted by new view transition` 뒤에 **렌더러가 죽었다**
(Page crashed — 320px에서 3/3 재현, 412px에서는 재현 안 됨). 크기만 320×568로 두고 프로필을
`Pixel 7`로 바꿨다. 실제 사파리는 `mobile-safari` 프로젝트가 본다.

**2. `waitingSnapshot`에 `hostId`가 없었다** (`game-flow` 4 · `disconnect` 2 · `create-room` 1).
실서버 계약에는 있고 `restSnapshot` 픽스처도 이미 넣고 있던 값이라 WS 픽스처만 빠져 있었다.

**3. `auth-login`이 앱이 만들지 않는 문구를 기다렸다** (모든 프로젝트 1건).
`「…님, 반가워요!」`는 `src` 어디에도 없다 — `AuthCallbackPage`가 <b>"성공했다는 인사는 따로
띄우지 않는다 — 헤더 아바타가 로그인 상태를 이미 말한다"</b>고 주석까지 남기고 지운 문구다.
스펙만 옛 동작에 남아 있었다. 계정 버튼(닉네임)을 보는 것으로 고쳤다.

이 셋은 **S15P11A406-167(e2e 스펙 복구)의 실질적인 내용**이었다.

---

## 🔴 남은 작업

**위의 "통과"는 아래 범위를 보증하지 않는다.**

### [ ] F-1. 아직 320px에서 보지 못한 화면

| 화면 | 상태 |
|---|---|
| 랜딩 · 닉네임 · 초대 오류 · 대기실 · 연습 게임 · **실전 게임 · 결과 화면** | 스펙으로 덮임 + 스크린샷 확인 |
| **파티 대시보드** — `PartyDashboardPage` · `PartyResultDashboard` | 320px에서는 **그려지지 않는다** — 라우터가 `<760px`에서 `PartyOnBigScreenPage`(안내 화면)로 받는다. 그 안내 화면은 확인했다 |
| **탁구 로컬 게임**(`/pingpong` — 187이 되살린 화면) | 스크린샷 확인(헤더 · 점수 · 코트 · 하단 안내 모두 320px에 들어간다) |
| **탁구 방 게임** — `PingPongGame`(실시간) | ❌ 미확인. `fakeGameServer`에 `PING_PONG` 계약이 없어 mock 흐름으로 그 화면까지 들어갈 수 없다 |
| **오디오 시트**(130이 가져온 화면) | 대기실·게임 양쪽 스크린샷 확인 |

실시간 탁구는 **별도 티켓이 맞다** — 스펙 하네스에 게임별 계약을 하나 더 붙이는 일이라 이
티켓의 좁은 폭 범위를 넘는다.

### [ ] F-2. 별도 티켓이 필요한 것

**타입 스케일 재설계** — 하드코딩 폰트 사이즈 **185건 / 29종**(`text-[Npx]`·`text-[Nrem]`,
clamp 제외). 크기 토큰은 `--ds-text-display` 하나뿐이고 `cn.ts`의 `theme.text`도 `['display']`
하나다. Tailwind 기본 스케일도 132건 병존해 **14px를 쓰는 방법이 `text-sm`과 `text-[14px]`
두 가지**다. 169가 「범위 밖 — 디자인 결정」으로 남긴 건.

커밋별 추이(같은 정규식으로 재측정):

| 시점 | 커밋 | 건수 / 종수 |
|---|---|---|
| 169 기준 | `ef52df7` | 149건 / 28종 |
| 169 병합 직후 | `6815671` | 145건 / 28종 |
| 현재 | `26c8233` | **185건 / 29종** |

**169 이후 42커밋 동안 40건 늘었다** — 스케일이 없으면 화면을 추가하는 사람이 눈대중으로 값을
정하는 것 말고 할 수 있는 일이 없다. 0.5px 변종도 4종(`10.5`·`12.5`·`13.5`·`14.5`) 16건 있다.

착수 시 주의: **`cn.ts`의 `theme.text` 목록도 같이 늘려야 한다.** 등록하지 않으면 `className`
오버라이드가 병합되지 않아 **169가 고친 문제가 타이포에서 재발한다**(그 파일 주석 참고).

**리액션 표정 SVG 시안** — B-2의 계열 B를 평면 SVG로 갈지. 계약(`ReactionType`)은 문자열만
흐르므로 서버 무관, 화면 표현만 바꾸면 된다.

### [ ] F-3. 범위 밖으로 둔 것 (되돌릴 필요 없음, 근거만 남긴다)

- **데스크톱 레이아웃** — 좁은 폭만 다뤘다. 1024px 이상 분기는 건드리지 않았다
- **`app/dev/` 화면** — `↑`·`←`·`→` 글리프가 남아 있지만 개발 전용이다
- **`LandingProgress`의 `transition-[width]`** — `baseline-ui`는 layout 애니메이션을 금하지만
  `fixing-motion-performance`가 "small, isolated surfaces"는 허용한다. 7→34px 막대라 예외에
  해당한다고 보고 두었다
- **`ReactionDock`의 `drop-shadow-[...]`** — 임의값이지만 3D 장면 위 이모지 가독성용이고
  `baseline-ui`의 해당 조항은 SHOULD다

---

## 새로 생긴 관습

| 것 | 위치 | 규칙 |
|---|---|---|
| 첫 진입 안내 | `GameDiceTray` | 코치마크와 모션 안내를 **같은 순간에 띄우지 않는다**. 새 안내를 더할 때도 순서를 정한다 — 겹치면 위층이 아래층의 닫기를 덮는다(G-1) |
| `Icon.tsx` | `shared/components/` | 20×20 `viewBox` · `currentColor` · `aria-hidden` 고정, `strokeWidth 1.8`. 아이콘 전용 버튼은 호출부가 `aria-label`을 단다. **새 글리프를 문자로 넣지 않는다.** 점은 `Dot`(칠한 원)으로 — 길이 0 선분 + 둥근 끝은 16px에서 사라진다 |
| `mobile-320` 프로필 | `playwright.config.ts` | 크기는 320×568, **프로필은 크로미움 계열**로 둔다. `devices['iPhone SE']`는 webkit 프로필이라 크로미움으로 돌리면 화면 전환에서 렌더러가 죽는다 |
| `--breakpoint-tiny` | `styles/tokens.css` | 320~359px **하한 전용**. `max-tiny:`로만 쓴다. 360px 이상 배치를 바꾸는 데 쓰지 않는다 |
| `rovingFocus.ts` | `shared/` | tablist·toolbar 공용 방향키 이동. 도메인마다 다시 구현하지 않는다 |
| `--lift` | `ReactionDock` · `tokens.css` | `DRIFTS`와 **서로소** 길이를 유지한다. 같은 길이로 만들면 조합이 5가지로 줄어 겹침이 돌아온다 |
| `mobile-320` | `playwright.config.ts` | 320px 하한 회귀 방지. 좁은 폭 스펙은 `NARROW_MAX_PX` 가드로 그 폭에서만 돈다 |
| 화면 프레임 | `LobbyPage` · `GameResult` | 높이는 `h-svh`로 고정하고 넘치면 `overflow-x-hidden`(세로 auto)으로 **프레임 안에서** 스크롤한다. **문서 높이를 늘리지 않는다** — `min-h-svh`는 화면 전환을 깨뜨린다 |
| 목록 하한 | `LobbyPage` · `GameResult` | 스크롤 목록에는 `min-h-*`를 준다. `flex-1`만으로는 짧은 화면에서 0에 가깝게 줄어들고, 높이를 못 받은 목록은 "비어 있다"로 읽힌다 |
| 폭에 따라 갈리는 라벨 | `GamePlayHeader` | 두 벌을 놓고 `max-tiny:hidden` / `hidden max-tiny:inline`으로 고른다. **스펙은 문구를 못 박지 말고** `flows.ts`의 `myTurnLabel`·`activeTurnLabel`(보이는 쪽만 고른다)을 쓴다 |

---

## 결정 기록

```text
- 날짜: 2026-08-04
- 변경한 결정: 초기 진단(break-keep 0건 · h3만 balance)을 폐기하고 원인을 레이아웃 압착으로 정정
- 근거: global.css :root에 keep-all·overflow-wrap이, h1/h2/h3에 text-wrap: balance가 이미 있다.
  파일을 중간부터 읽고 유틸리티만 grep한 내 실수였다.
- 영향: 티켓 본문과 이 문서에 정정 절을 남겼다. 조치는 max-tiny breakpoint로 바뀌었다.
- 후속 작업: 없음
```

```text
- 날짜: 2026-08-04
- 변경한 결정: 아이콘 교체 범위를 9곳 → 12곳으로 넓힘(? · ! 추가)
- 근거: ✕만 SVG로 바꾸고 옆의 ?를 문자로 두면 같은 HeaderButton 줄에서 일관성이 깨진다.
  사용자 지시가 "a 같은 ui성 이모지"였으므로 같은 종류로 해석했다.
- 영향: InAppBrowserGate의 checklistTone이 glyph 문자열 → Icon 컴포넌트 참조로 바뀐다.
- 후속 작업: 없음
```

```text
- 날짜: 2026-08-04
- 변경한 결정: 리액션 픽커가 보낸 뒤에도 열려 있게 바꿈(기존 테스트의 의도를 뒤집음)
- 근거: 기존 테스트 주석은 "보내면 픽커가 닫힌다 — 게임 화면을 계속 덮고 있으면 안 된다"였다.
  그 의도는 유효하지만, 당시에는 자동 닫힘이 유일한 닫는 수단이었다. 바깥 누르기·Escape·
  트리거 재클릭을 추가했으므로 자동 닫힘 없이도 의도가 지켜진다.
- 영향: 연타가 가능해진다. 기존 테스트 1건을 새 동작 기준으로 고치고 5건을 추가했다.
- 후속 작업: 실기기에서 픽커가 게임 화면을 얼마나 가리는지 확인(E-1)
```

```text
- 날짜: 2026-08-04
- 발견 → 해소: 브라우저 계산값으로 "셰브론이 회전하지 않는다"고 오진단
- 원인: 창이 표시되지 않아 document.timeline.currentTime이 0으로 멈춰 있었다. 진행 중인
  rotate 트랜지션의 시작값을 최종값으로 읽었다. transitionProperty를 none으로 끄면 180deg가
  나온다.
- 영향: 근거 없는 주석과 rotate-0 클래스를 넣었다가 되돌렸다. 코드는 원래대로다.
- 후속 작업: 이 환경에서 애니메이션 의존 측정을 하지 않는다(문서 상단 교훈 참고)
```

```text
- 날짜: 2026-08-04
- 발견 → 해소: labelFit 단정이 헛돌았다 — 수정을 떼도 통과했다
- 원인: 처음엔 줄 수만 셌다. nowrap만 걸어도 줄 수는 1이 되고, 대신 라벨이 칸을 삐져나와
  옆 버튼과 겹친다. 그 겹침은 뷰포트 안이라 넘침 검사에도 걸리지 않는다.
- 영향: scrollWidth와 부모 오른쪽 끝을 함께 보게 고쳤다. 수정을 떼면 fits: false로 실패한다.
- 후속 작업: 새 단정을 넣을 때마다 수정을 떼어 실패를 확인한다
```

```text
- 날짜: 2026-08-04
- 변경한 결정: 모션 안내 자동 열림(182)을 "코치마크가 닫힌 뒤"로 미뤘다
- 근거: 두 안내가 같은 순간에 뜨면 z-30인 모션 안내가 코치마크의 「알겠어요」를 덮어
  첫 진입 사용자가 코치마크를 닫을 수 없다(Playwright의 pointer 차단으로 재현).
  둘 중 스스로 닫히는 쪽(모션 안내는 3초)을 뒤로 보내면 두 의도가 다 산다.
- 영향: 182가 남긴 단정 3건의 문구·순서를 고쳤다. 재방문자(코치마크를 이미 닫은 사람)는
  예전과 같이 마운트와 함께 모션 안내를 본다.
- 후속 작업: 없음. 안내를 더 더할 때는 순서를 정해서 넣는다.
```

```text
- 날짜: 2026-08-04
- 변경한 결정: 결과·대기실 화면을 min-h-svh(페이지 스크롤)로 늘리려던 것을 되돌리고
  h-svh + overflow-x-hidden(프레임 안 스크롤)으로 갔다
- 근거: 처음에는 "min-h-svh를 쓰면 렌더러가 죽는다"고 판단했다 — **그 진단은 틀렸다.**
  크래시의 원인은 mobile-320 프로젝트가 사파리 프로필로 크로미움을 돌리던 것이었고,
  프로필을 Pixel 7로 고친 뒤에는 min-h-svh로도 15건이 전부 통과한다(재확인했다).
  그래도 프레임 고정을 유지한다: 이 앱의 모든 화면은 정확히 한 뷰포트를 프레임으로 잡고
  (GamePlay의 3D 트레이가 그 프레임에 맞춰 크기를 잡는다), 문서가 자라는 화면을 하나만
  섞으면 화면마다 스크롤 주체가 달라진다.
- 영향: 목록 하한(min-h-28)은 그대로. 보이는 결과는 같다(순위 두 줄·참가자 카드가 보인다).
- 후속 작업: 없음. 페이지 스크롤로 바꾸고 싶다면 앱 전체를 함께 바꾸는 별도 판단이다.
```

```text
- 날짜: 2026-08-04
- 발견 → 해소: 320px 크래시를 "min-h-svh 탓"으로 오진단했다
- 원인: devices['iPhone SE']는 defaultBrowserType이 webkit인 프로필인데 브라우저를 지정하지
  않은 프로젝트에서 크로미움으로 돌아간다. 그 조합에서 화면 전환이 겹치면 렌더러가 죽는다.
  같은 크래시로 invalid-invite 2건·smoke 1건이 이 브랜치 전부터 실패하고 있었는데,
  내 변경으로 대기실·결과 화면에서도 터지자 원인을 레이아웃으로 착각했다.
- 영향: 프로젝트 프로필을 Pixel 7(크기는 320×568 유지)로 고쳐 3건이 되살아났다.
- 후속 작업: 기기 프로필을 쓸 때 defaultBrowserType과 실행 브라우저가 맞는지 본다.
```

```text
- 날짜: 2026-08-04
- 변경한 결정: e2e 픽스처 waitingSnapshot에 hostId를 넣었다(167의 땅을 한 줄 건드렸다)
- 근거: 이 티켓의 남은 작업(E-2 실전 게임·결과 화면 확인)이 그 한 줄에 막혀 있었다.
  실서버 계약에는 hostId가 있고 restSnapshot 픽스처는 이미 HOST.id를 넣고 있었다 —
  WS 픽스처만 빠져 있던 불일치다.
- 영향: game-flow 4건 · disconnect 2건 · create-room 1건이 되살아났다. 참가자 시점 스펙은
  you=GUEST라 그대로 비호스트로 남는다.
- 후속 작업: 167에 남은 것은 auth-login·invalid-invite·smoke 4건이다.
```

```text
- 날짜: 2026-08-04
- 변경한 결정: 헤더 턴 라벨을 320px에서 짧게 갈랐고, 그 결과 문구를 못 박은 e2e 단정 7곳을
  flows.ts 헬퍼(myTurnLabel · activeTurnLabel)로 바꿨다
- 근거: 폭에 따라 문구가 갈리면 문구를 박은 단정은 mobile-320 프로젝트에서만 실패한다.
  같은 스펙이 프로젝트마다 다르게 도는 것은 스펙 쪽 결함이다.
- 영향: disconnect · game-flow · reconnect · participant-view 4개 파일. 단정의 뜻은 그대로다.
- 후속 작업: 없음
```

```text
- 날짜: 2026-08-04
- 발견 → 해소: 리액션 넘침 단정이 처음엔 헛돌았다(고친 코드를 되돌려도 통과)
- 원인: 감싸는 span은 right-0 · w-tap이라 언제나 독 안에 있다. 삐져나가는 것은 그보다 넓은
  닉네임 필이므로 안쪽 조각을 재야 했다. 또 리액션을 두 개만 띄우면 DRIFTS의 음수 값만
  검사한 셈이 된다 — 다섯 개를 띄워 모든 값을 쓴다.
- 영향: 단정을 안쪽 span 기준으로 고치고 리액션을 5개 보내게 했다. 되돌리면 5건 모두 실패한다.
- 후속 작업: 없음
```

```text
- 날짜: 2026-08-04
- 사건: game-flow.spec.ts 4건이 실패하는 것을 발견
- 원인: 이 브랜치와 무관하다. waitingSnapshot에 hostId가 없어 mock 흐름에서 호스트가 되지
  않고, startHostedGame이 찾는 '게임 시작' 버튼 이름이 '게임 시작 · 호스트 전용'이 된다.
  LobbyPage는 이 브랜치에서 건드리지 않았다.
- 영향: 게임 화면 320px 검증을 /tutorial 경로로 우회했다. 실전 전용 요소는 덮이지 않는다.
- 후속 작업: (해소) hostId 픽스처를 채워 실전 흐름을 열었다 — 위의 결정 기록 참고
```
