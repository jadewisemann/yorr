# S15P11A406-215 반복 엘리먼트 제거 — 화면 셸·공통 컴포넌트 추출

> Jira: https://ssafy.atlassian.net/browse/S15P11A406-215
> Branch: `refactor/215-element-dedup`
> 기준 브랜치: `develop` (`7ed0765`)
> 최종 갱신: 2026-08-05

목표는 **같은 UI를 더 적은 코드로**다. 줄 수를 줄이는 것 자체가 아니라 **같은 결정이 한 곳에만
적히게** 만드는 것이다.

원칙: 기준을 먼저 못 박고, 각 기준은 grep으로 판정 가능한 형태로 쓴다. 취향으로 적으면
리뷰에서 다시 논쟁이 된다.

---

## 1. 확정 기준

### 1.1 컴포넌트는 렌더링만 한다

컴포넌트 함수 본문에 남는 것: 구조분해, 한 줄짜리 파생 표시값, JSX. 그 외는 나간다.

| 무엇 | 어디로 |
|---|---|
| `useState` 여러 개가 하나의 상태를 이룰 때 | 같은 폴더의 `use<이름>.ts` |
| `useEffect` — 구독·타이머·리스너·측정 | 훅 |
| 계산·포맷·정렬·매핑 | 같은 도메인의 순수 모듈 (`duel.ts` · `court.ts`처럼) |
| API 호출 | `<도메인>/api/use*.ts` |

**판정선**: 화면·컴포넌트 파일 하나에 훅 호출(`useState`·`useEffect`·`useLayoutEffect`·`useRef`·
`useMemo`·`useCallback`·`useReducer`) **6개를 넘으면 분리 대상**이다.

순수 모듈은 React·DOM을 import하지 않는다. `yacht/domain/`에 이미 걸려 있는 규칙을
`duel/`·`pingpong/`에도 적용한다.

### 1.2 공통 컴포넌트를 내려받아 일부만 고친다

특화 컴포넌트는 공통 컴포넌트를 **다시 만들지 않고 감싼다.**

```tsx
// 좋다 — Button을 감싸 기본값만 고정한다. variant·disabled·loading·ref가 그대로 통과한다.
export function PrimaryCta({ className, ...props }: ComponentProps<typeof Button>) {
  return <Button size="cta" className={cn('w-full', className)} {...props} />
}

// 나쁘다 — 같은 모양을 <button>으로 다시 짠다. focus-ring·disabled·loading이 빠진다.
export function PrimaryCta(props) {
  return <button className="min-h-[3.625rem] rounded-panel bg-brand ..." {...props} />
}
```

- `className`은 **항상 마지막에 `cn()`으로 병합**한다. 호출자가 이길 수 있어야 한다.
- props는 `...props`로 통과시킨다. 필요한 것만 골라 받으면 확장할 때마다 파일을 고쳐야 한다.
- 새 variant가 필요하면 감싸는 쪽에 스타일을 쌓지 말고 **공통 컴포넌트의 variant map에 추가**한다.
  `Button`의 `cta` size가 그렇게 승격된 선례다.
- 배치(`w-full` · `mt-4`)는 호출부, 내부 구조와 상태 표현은 컴포넌트가 소유한다.

### 1.3 설정보다 조합

props 5개 이상으로 내부 구조를 조립하는 컴포넌트는 children을 받는 형태로 바꾼다.
`title`·`headerVariant`·`bodyPadding`·`content` 같은 props 묶음이 신호다.

### 1.4 이름

- 축약하지 않는다. `idx` → `index`, `btn` → `button`, `cfg` → `config`, `e` → `event`.
  예외는 관례가 굳은 것(`ref`·`id`·`props`)뿐이다.
- boolean은 `is`/`has`/`can`/`should` 접두. 단 HTML 속성과 짝인 것(`disabled`·`loading`)은 그대로.
- 핸들러는 prop `on<사건>` / 구현 `handle<사건>`.
- 훅 반환은 객체 구조분해. 배열은 `useState` 흉내를 낼 때만.
- **이름이 설명을 필요로 하면 이름이 틀린 것이다.** 주석을 붙이는 대신 이름을 고친다.

### 1.5 주석 — 무엇(what)은 지우고 왜(why)만 남긴다

주석을 줄이는 방법은 지우는 게 아니라 **코드가 스스로 말하게 만드는 것**이다.

지운다:

- 코드를 한국어로 다시 읽어주는 주석 (`// 상태를 초기화한다`)
- 이름을 고치면 사라지는 주석
- 내용 없는 TODO, 변경 이력

남긴다 — **다음 사람이 이 줄을 "단순화"하다 버그를 만들 수 있는 곳만**:

- 대안을 시도해서 실패한 기록 (`recipes.css`의 `@layer components` vs `@utility`,
  `cn.ts`의 `extendTailwindMerge` 목록이 왜 필요한지)
- 물리·타이밍·브라우저 특성에 의존하는 값 (`h-svh` vs `min-h-svh` 결정, iOS 주소창과 `dvh`,
  rapier 스텝과 CSS 애니메이션의 경계)
- 와이어 계약·외부 규정 (카카오 브랜드 색)

**형식**: why 주석은 **3줄 이내 한 문단.** 지금 20줄짜리 블록 주석은 그 자체가 리팩터 대상이다 —
설계 근거는 `docs/`로 옮기고 코드에는 링크 한 줄만 남긴다.

> **이 항목은 조정의 결과다.** 「주석 적게」가 요청이지만 현재 주석 중 상당수는 실제 디버깅으로
> 얻은 결론을 담고 있어서(이전 티켓의 `.dev.md`가 그 증거다) 통째로 지우면 같은 함정을 다시
> 밟는다. 그래서 **양을 줄이되 근거는 문서로 이동**시킨다. 이 판단을 뒤집고 싶으면 여기를 고친다.

### 1.6 폴더 구조 — 도메인 우선을 유지한다

`src/` 바로 아래는 도메인, 레이어는 도메인 **안에** 둔다. 기존 [`CLAUDE.md`](CLAUDE.md) 규칙 그대로.

```
src/<도메인>/
  screens/        라우트가 직접 그리는 화면
  components/     그 도메인 전용 컴포넌트
  api/            REST 훅
  <이름>.ts       순수 모듈 (React 모름)
  use<이름>.ts    도메인 훅
  __tests__/      위 전부의 테스트
```

컴포넌트당 폴더(`components/TaskList/TaskList.tsx`)는 **쓰지 않는다.** 한 컴포넌트가
`Foo.tsx` + `useFoo.ts` + `types.ts` + 테스트 넷 이상으로 갈라질 때만 만든다. 현재 그런 건 없다.

> 일반 프론트엔드 가이드는 컴포넌트 우선 콜로케이션을 권하지만, 이 저장소는 도메인 우선을 택한
> 이유가 기록되어 있다 — 「레이어를 먼저 두면 야추 하나를 이해하려고 6개 폴더를 동시에 뒤져야
> 했다」. 게임이 3개로 늘어난 지금 그 판단은 더 맞다. 바꾸지 않는다.

### 1.7 원시 값 금지

- 색: `bg-[#070b12]` 금지. `--ds-*` 원시값을 정의하고 `@theme inline`으로 semantic token에 연결한다.
- `text-white`도 원시값이다 → `text-content` 계열.
- 간격·반경·그림자는 토큰. 임의 값은 safe-area·fluid typography처럼 토큰화가 부적합할 때만.

토큰을 추가하면 [`src/shared/cn.ts`](src/shared/cn.ts)의 `extendTailwindMerge` 목록도 같이 고친다.
안 고치면 컴포넌트 기본값과 호출자 override가 둘 다 살아남아 승자를 CSS 선언 순서가 정한다 —
기준 1.2가 성립하지 않는다.

### 1.8 파일 크기

화면·컴포넌트 파일 **200줄 초과는 분해 대상.** 순수 모듈·테스트는 예외.

### 1.9 접근성 — 기존 수준을 낮추지 않는다

리팩터로 잃기 쉬운 것들이라 체크리스트로 둔다. 새로 만드는 게 아니라 **유지**가 목표다.

- 클릭 가능한 요소는 `min-h-tap` 이상, `<button>`/`<a>` 사용 (`<div onClick>` 금지)
- focus-visible 링을 지운 채로 커밋하지 않는다 (`focus-ring` 레시피)
- 아이콘 전용 버튼에 `aria-label`
- 상태를 색으로만 표시하지 않는다
- loading / error / empty 세 상태를 다 그린다
- 애니메이션은 `motion-reduce`에서 개별 중화한다 (전역 스위치로 끄지 않는다)

---

## 2. 기존 문서와의 관계

| 문서 | 이 리팩터에서의 취급 |
|---|---|
| [`CLAUDE.md`](CLAUDE.md) | **유효.** 도메인 우선 구조 · 모션 경계 · 검증 최소화 원칙 유지 |
| [`docs/engineering/design-system.md`](docs/engineering/design-system.md) | **현황 기술로 취급.** 코드를 먼저 바꾸고 문서를 뒤따라 갱신 |
| [`docs/engineering/architecture-and-stack.md`](docs/engineering/architecture-and-stack.md) | 의존 방향(`app → landing → room → yacht`) 유효 |

### shadcn/ui는 도입하지 않는다

- 대체 대상(`Button` 63줄 · `Modal` 115 · `Popover` 215 · `BottomSheet` 161 · `Tooltip` 135 ·
  `TextField` 55 · `ToastHost` 44 = **788줄 + 테스트 711줄**)이 이 저장소에서 **중복이 가장 적고
  테스트가 가장 촘촘한 영역**이다. `Button`은 이미 variant×size 정적 map으로 63줄이고 shadcn
  등가물은 55줄 — 8줄 아끼려고 CVA를 들이는 셈이다.
- `--ds-*` semantic 토큰과 `cn.ts`의 tailwind-merge 충돌 그룹 등록을 shadcn 토큰 체계로 바꾸면
  전 파일 마이그레이션이 딸려온다.
- 모든 오버레이가 `AnimatePresence` + `shared/motion.ts` variants로 퇴장을 그린다. Radix는
  마운트를 자기가 소유하므로 `forceMount` 수동 배선 + jsdom mock 추가가 필요하다.
- 커스텀 요구가 Radix 기본형을 벗어난다 — `Popover`의 앵커 없는 모드(랜딩 헤더 고정 자리),
  꼬리 x좌표와 `transformOrigin` 일치, 꼬리가 잘리지 않게 패널이 아니라 **내용만** 스크롤.

### 단, `Popover` 포지셔닝은 별개 티켓으로 남긴다

[`Popover.tsx:112`](src/shared/components/Popover.tsx#L112)가 `resize`만 듣고 **`scroll`을 듣지
않는다** — 앵커가 스크롤로 움직이면 팝오버가 따라가지 않는다. `@floating-ui/react`만 넣으면 이
버그와 손코딩 `placeByAnchor` 약 80줄이 함께 정리된다. shadcn·CVA·나머지 6개 컴포넌트는 건드리지
않는다. **이 티켓 범위 밖이다.**

---

## 3. 착수 시점 실측 (`develop@7ed0765`)

| 항목 | 값 |
|---|---|
| `src/` ts+tsx | 324 파일 / 51,356줄 (tsx만 17,872줄) |
| **200줄 초과 tsx** (기준 1.8 위반) | **31 파일** |
| **화면 셸 손코딩** | **25곳** (4.2의 분류) |
| **공통 `Button` 미사용 생 `<button>`** | **68곳** |
| **훅 6개 초과 파일** (기준 1.1 위반) | **7개** |
| class에 박힌 원시 hex | **아래 정정 참고 — 최초 측정 76회는 심한 과소집계였다** |

### ⚠️ 색 실측 정정 — 최초 76회는 틀렸다

최초 grep은 `.tsx`의 `[#...]` Tailwind 클래스만 봤다. 세 갈래 조사로 실제 규모를 다시 재니
**성격이 다른 문제 두 개가 섞여 있었다.**

grep이 놓친 것:

- **`Arena.tsx`에는 arbitrary-hex Tailwind 클래스가 0개다.** 전부 인라인 `style`의 그라디언트고
  색이 **150개 이상**이다 — 페이즈별 하늘·땅 그라디언트, 메사, 선인장, 회전초, 신호 램프 렌즈,
  놋쇠 탄피 HP 핍, 예광탄.
- `Gunslinger.tsx`는 SVG `fill` 30개, Tailwind 0개. `pingpong/scene3d.ts`는 WebGL 재질 25개.
- `rgb(255 207 138 / 10%)` 같은 공백 구분 표기는 hex grep에 걸리지 않는다 — duel 16개, pingpong 7개.
- `${color}99` · `'0 0 24px #ff7a4d99'` 처럼 **알파를 문자열로 이어붙인 것**은 hex grep에도 안 걸리고
  `var()`로 치환도 **불가능하다** (`var(--x)99`는 문법이 아니다).
- `text-red-300` · `text-amber-200` 같은 Tailwind 기본 팔레트 색이 `#fca5a5` · `#fbbf24` 바로 옆에서
  같은 역할(danger·warning)을 하고 있다 — 어느 grep에도 안 걸린다.

**결론이 갈린다:**

| | 성격 | 조치 |
|---|---|---|
| `Arena.tsx` · `Gunslinger.tsx` · `scene3d.ts` · 모든 그라디언트 스톱 | **일러스트레이션이다.** 손으로 그린 2D 서부 배경과 캐릭터 SVG, WebGL 재질. 값과 순서가 그림 자체다 | **토큰화하지 않는다.** 범위 밖 |
| `duel/`·`pingpong/`의 UI 크롬 — 페이지 캔버스·카드 표면·상태 피드백 색 | 실제로 파일을 넘어 재사용되는 semantic 색 | 토큰화 대상 (Phase 1b) |
| `duel/`·`pingpong/` **밖** | arbitrary hex **0개.** 대신 흰색 알파 사다리가 두 벌 | 토큰화 대상 (Phase 1a) |

### 깨면 안 되는 불변식 — three.js ↔ CSS 짝

| 값 | CSS 쪽 | three.js 쪽 |
|---|---|---|
| `#070b12` | `bg-[#070b12]` 9곳 | `setClearColor` **그리고** `FogExp2` — 두 곳이 함께 움직여야 한다 |
| `#2b8fe0` | 클래스 4곳 + `rgb(43 143 224)` | `P1_COLOR` — 「기존 2D 색 유지」주석이 달려 있다 |
| `#e2513c` | 클래스 3곳 | `P2_COLOR` |
| `#ffd24a` | CSS 6곳 | perfect 타이밍 링 |

CSS 쪽만 토큰화하면 이 짝이 **조용히 깨진다.** 그리고
`pingpong/__tests__/feedback.test.ts:21-24`가 `comboStyle`의 hex 4개를 문자열로 못 박아 뒀다.

### 한 hex가 두 역할을 하는 곳 — 단일 토큰으로 합치면 안 된다

- `#ff8b7c` = P2 팀 색(점수·이름) **그리고** 실책 피드백 텍스트. 팀 정체성 vs 실패 상태
- `#49e08a` = 준비 완료 상태 **그리고** 로컬 게임 기본 피드백 **그리고** 3D 타이밍 링
- `#ffd24a` = NICE 피드백 **그리고** 듀스·매치포인트 라벨 **그리고** 콤보 중간 티어 **그리고** 타이밍 링
- `#73bfff` = P1 점수 색 **그리고** WARM-UP 눈썹(팀 의미 없음)
- `#0b0409` = 듀얼 페이지 캔버스 **그리고** 캐릭터 몸통 그라디언트 0% 스톱
- `#86efac` = 「승리」본문 **그리고** 램프 렌즈 스톱 **그리고** 초록 태양

### 조사가 덤으로 찾은 별건 3개 (이 티켓 범위 밖, 티켓만 남긴다)

1. **`DuelGame.tsx:547`이 무승부를 승리로 그린다.** 승리 초록 `#86efac`을 하드코딩했는데 그 heading의
   텍스트가 `무승부`일 수 있다. `#86efac → positive` 치환 패스는 이 버그를 굳힌다.
2. **`physics-dice/appearance.ts`의 fallback이 죽은 팔레트다.** `--ds-color-physics-accent`는
   `#ff4d48`(레드)인데 fallback은 `#c6f640`(라임), rail은 `#0d0e10` vs `#1e2941`(네이비).
   CSS 변수 해석이 실패하면 트레이가 옛 라임/네이비 테마로 렌더된다.
3. **중복 위젯이 이미 갈라졌다.** `Arena.tsx:636`은 탄피 테두리에 `white/18`, 같은 위젯을 다시 구현한
   `DuelGame.tsx:604`는 `white/16`. 램프도 `DuelController`와 `Arena`가 초록 렌즈 스톱 수가 다르다
   (3 vs 4). Phase 6의 실증 근거다.

### 훅 밀도 초과

| 파일 | 훅 | 줄 |
|---|---|---|
| `yacht/components/PhysicsDiceScene.tsx` | 16 | 271 |
| `yacht/screens/GamePlay.tsx` | 15 | 784 |
| `duel/DuelGame.tsx` | 14 | 614 |
| `pingpong/PingPongGame.tsx` | 12 | 751 |
| `pingpong/PingPongModePage.tsx` | 9 | 358 |
| `yacht/components/ReactionDock.tsx` | 8 | 296 |
| `landing/screens/EntryPage.tsx` | 7 | 587 |

### 도메인별 편차가 크다

훅 파일이 `yacht/` 5개(`useGamePlayRoll` · `useGamePlaySubmission` · `useLocalRoomSnapshot` ·
`useMotionRollInput` · `useCountdown`), `room/` 2개, **`duel/`과 `pingpong/`은 0개**다.
그리고 두 폴더만 레이어 구분 없이 플랫이라 `Arena.tsx`(1011줄)가 도메인 루트에 있다.

**즉 기준 1.1·1.6은 이미 `yacht/`에서 실행된 패턴이다.** 새 규약을 발명하는 게 아니라
`duel/`·`pingpong/`을 같은 수준으로 끌어올리는 일이다.

---

## 4. 계획

각 Phase = 커밋 1개. 아래 순서에는 의존이 있다 — **Phase 1 없이 Phase 2를 하면 셸을 공통화할 수
없다.** 원시 hex가 도메인마다 달라서 셸 문자열이 갈라져 있기 때문이다.

### Phase 1 — 토큰 정리 (선행 조건)

**하드 제약: 외관을 바꾸지 않는다.** 목표가 「동일한 UI」다. 그래서 기존 `positive`(`#8fcb9b`)에
duel/pingpong의 형광 초록(`#49e08a`)을 흡수시키지 않는다 — 값이 다르면 색이 변한다.
**정확한 값을 보존한 채 이름만 준다.**

#### [x] Phase 1a — 흰색 알파 사다리 통합 (`duel/`·`pingpong/` 밖)

이 범위에 arbitrary hex는 0개다. 문제는 같은 사다리가 두 벌 적혀 있는 것:

| 값 | 앱 네임스페이스 | landing 네임스페이스 | 사이트 |
|---|---|---|---|
| `white/10` | 토큰 `border` | `landing-hairline` (**같은 값**) | 정의 중복 |
| `white/18` | **토큰 없음** | `landing-hairline-strong` (**같은 값**) | **12곳** |
| `white/14` | **토큰 없음** | 없음 | **7곳** |
| `white/6` | 토큰 없음 | `landing-soft`(7% — 1% 차이) | 5곳 |

`landing-*`가 별도인 이유는 랜딩 팔레트가 다르기 때문인데, **중성 흰색 알파 hairline은 팔레트가
아니라 같은 사다리를 두 번 적은 것**이다. 색 토큰은 네임스페이스를 유지하고 **hairline 사다리만
합친다.** `tokens.css`의 「반투명 패널·헤어라인은 랜딩에서만 쓰므로」주석은 이제 사실이 아니다 —
`white/18`이 랜딩 밖 12곳에서 쓰인다.

신설 토큰 3개 (전부 기존 값과 **정확히 일치**하므로 외관 변화 0):

| 토큰 | 값 | 역할 | 사이트 |
|---|---|---|---|
| `border-raised` | `white/14` | 떠 있는 표면의 경계 — 시트 상단, 툴팁, 칩, 점선 빈자리 | 7 |
| `border-strong` | `white/18` | 강조·선택 상태 경계, 그리고 1px 구분선 | 12 |
| `surface-veil` | `white/6` | 캔버스 위 아주 옅은 칩·배너 바닥 | 5 |

`--ds-landing-hairline`/`-strong`는 신설 원시값을 참조하게 바꾼다 — **호출부를 건드리지 않고
정의만 하나로 만드는** 단계다. 6%와 7%는 합치지 않는다(1% 차이지만 외관 보존이 우선).

`cn.ts`는 **고치지 않는다.** 그 파일 주석이 이미 말한다 — 「색 토큰은 tailwind-merge의 색
validator가 임의 이름을 받아주므로 등록하지 않아도 병합된다」.

**완료 조건**: `duel/`·`pingpong/`·`app/dev/`·QR·three.js 밖에서 `white/(6|14|18)` 0건.

#### [ ] Phase 1b — duel/pingpong의 UI 크롬만

파일을 넘어 재사용되는 semantic 색만 이름을 준다. 값은 그대로.

- 페이지 캔버스: `#070b12`(탁구, 9곳) · `#0b0409`(듀얼, 4곳)
- 카드 표면: `#0b111b`(탁구, 2곳 — 알파가 `/95`와 불투명으로 갈려 있다)
- 준비/연결 쌍: `#49e08a` + `#8dffc0` (테두리 알파가 `/45`와 `/40`으로 갈려 있다)
- 팀 색 쌍: `#2b8fe0`/`#73bfff`(P1) · `#e2513c`/`#ff8b7c`(P2)

**하지 않는 것** — 범위를 여기서 못 박는다:

- `Arena.tsx`의 배경·풍경·램프·탄피·예광탄 (일러스트레이션)
- `Gunslinger.tsx`의 SVG fill 전부 (캐릭터)
- `scene3d.ts`의 WebGL 재질·라이트·CanvasTexture 전부
- `${color}99` 형태의 문자열 알파 연결 9곳 (`var()`로 치환 불가)
- `text-red-300`·`text-amber-200` → 별건. 팔레트 색과 hex가 같은 역할을 하는 문제는
  Phase 1b가 아니라 danger/warning 정책 자체를 정할 때 함께 본다

three.js 짝이 있는 색(`#070b12`·`#2b8fe0`·`#e2513c`·`#ffd24a`)은 토큰을 만들되 **three.js 쪽 상수에
「CSS 토큰과 같은 값을 양쪽에 적어둔 것」주석을 남긴다** — `motion.ts`와 `tokens.css`가 duration을
양쪽에 적어두고 주석으로 묶어 둔 것과 같은 방식이다. 값을 한쪽만 고치지 않게 한다.

**완료 조건**: 위 목록의 색이 토큰이 되고, `feedback.test.ts`의 hex 단정이 그대로 통과한다.

### [ ] Phase 2 — 화면 셸 컴포넌트

25곳의 셸을 네 가족으로 정리한다. 무엇이 실제로 다른지 확인한 결과:

| 가족 | 현재 위치 | 실제로 다른 것 |
|---|---|---|
| **문서 흐름 화면** `min-h-dvh` + safe-area 상하 패딩 | `InAppBrowserGate` · `NotFoundPage` · `InvalidInvitePage` · `NicknamePage` · `PartyOnBigScreenPage` (5) | `max-w`(lg/2xl), `pt`(1rem/2.5rem) |
| **단일 뷰포트 화면** `h-svh` + `overflow-hidden` | `LobbyPage` · `GameResult` · `LeveragePage` · `EntryPage`×2 · `PingPongModePage` · `PingPongGame`×3 · `DuelGame`×3 · `DuelController` · `PingPongController`×2 (16) | `max-w`, 배경, `touch-none`/`select-none` |
| **게임판** `mx-auto h-svh w-full max-w-play overflow-hidden bg-canvas text-content` | `GamePlay` · `PartyDashboardPage` · `PartyResultDashboard` (3) | **없다 — 문자열이 완전히 동일하다** |
| **중앙 정렬 상태 화면** | `ScreenFallback` · `AuthCallbackPage` · `DuelGame:258` · `PingPongGame:171` (4) | 배경, `role`/`aria-busy` |

`shared/components/Screen.tsx`에 `<Screen>`을 두고 네 가족을 variant로 받는다. `max-w`·배경은
props가 아니라 `className`으로 확장한다(기준 1.2). **safe-area 패딩 계산은 컴포넌트가 소유한다** —
지금 25곳이 각자 `pb-[max(1.25rem,env(safe-area-inset-bottom))]`를 손으로 쓰고 있다.

`h-svh` vs `min-h-svh` 결정은 **바꾸지 않는다.** 이 앱의 모든 화면은 정확히 한 뷰포트를 프레임으로
잡고 `GamePlay`의 3D 트레이가 그 프레임에 맞춰 크기를 정한다. 근거는 이전 티켓의 결정 기록에 있고,
`Screen.tsx`에 링크 한 줄로 남긴다(기준 1.5).

**완료 조건**: `env(safe-area-inset` 문자열이 `Screen.tsx`와 토큰 밖에 나타나지 않는다.

### [ ] Phase 3 — 생 `<button>` 68곳 수렴

`<Button>`으로 바꾼다. 바꿀 수 없는 것은 두 갈래로 처리한다:

- 반복되는 것(`reaction-chip` · `quick-chip`)은 이미 `recipes.css`에 레시피가 있다 →
  그 레시피를 쓰는 얇은 컴포넌트로 승격
- 진짜 일회성(전체화면 스크림 등)은 그대로 두되 `focus-ring` + `min-h-tap`은 반드시 지킨다

`Button`에 variant/size가 부족하면 **여기서 추가한다.** 감싸는 쪽에 스타일을 쌓지 않는다(기준 1.2).

**완료 조건**: 남은 생 `<button>` 각각에 「왜 Button이 아닌지」가 이름이나 레시피로 드러난다.

### [ ] Phase 4 — 훅·유틸 분리

3절 표의 7개 파일. `yacht/screens/useGamePlayRoll.ts`가 이미 있는 패턴이니 그대로 확장한다.

- `PhysicsDiceScene`(훅 16개) — three.js/rapier 수명주기를 `useDiceScene.ts`로.
  순수 씬 구성은 `rendering/physics-dice/`의 모듈로.
- `DuelGame` · `PingPongGame` — 상태 기계를 `duel.ts`/`localGame.ts`(이미 있다)로 밀고
  구독·타이머만 `useDuelGame.ts`/`usePingPongGame.ts`에 남긴다.
- `EntryPage` — 히어로 연출 상태를 `useEntryHero.ts`로.

**주의**: 모션 경계를 넘지 않는다. 돌아가는 CSS 애니메이션(`callout-*` · `ring-pulse`)을
motion으로 옮기지 않는다 — rapier 스텝과 같은 프레임을 다투게 된다.

### [ ] Phase 5 — `duel/`·`pingpong/` 폴더 구조

두 도메인만 플랫이다. `yacht/`와 같은 모양으로 맞춘다.

```
duel/                            pingpong/
  screens/DuelGame.tsx             screens/PingPongGame.tsx
  screens/DuelController.tsx       screens/PingPongModePage.tsx
  components/Arena.tsx             screens/PingPongController.tsx
  components/Gunslinger.tsx        components/PingPongControllerHowTo.tsx
  components/DuelHowTo.tsx         api/pingPongAiResultApi.ts
  duel.ts stage.ts sounds.ts       court.ts localGame.ts feedback.ts …
```

**import 이동만 하는 커밋으로 분리한다** — 로직 변경과 섞으면 리뷰가 불가능해진다.

### [ ] Phase 6 — 200줄 초과 31개 분해

Phase 1~5를 하면 상당수가 자연히 내려온다. 남는 것을 큰 순서로:
`Arena` 1011 → `TutorialGuide` 804 → `GamePlay` 784 → `PingPongGame` 751 → `DuelGame` 614 →
`EntryPage` 587 → `PingPongController` 505 → `LobbyPage` 493.

분해 기준은 줄 수가 아니라 **관심사**다. 한 파일이 그리는 화면 상태가 여럿이면
(`PingPongGame`이 대기·플레이·결과를 한 파일에서 그린다) 상태별로 가른다.

### [ ] Phase 7 — 마무리

- `recipes.css` 재점검. Phase 2·3 뒤에 「DOM 5회 이상」 기준을 다시 적용한다 —
  `Screen`·`Button`으로 흡수된 레시피는 지운다.
- 20줄 넘는 블록 주석을 `docs/`로 이동하고 코드에는 링크 한 줄만 남긴다(기준 1.5).
- 확정 정책을 `design-system.md`·`CLAUDE.md`에 반영한다.

---

## 5. 검증

[`CLAUDE.md`](CLAUDE.md)의 테스트 최소화 원칙을 따른다. Phase별로 필요한 것만 돌린다.

| Phase | 검증 |
|---|---|
| 1 토큰 | `npm run typecheck` · `npm run build` · 위 grep 0건 |
| 2 셸 | 위 + `/__dev/components`에서 320·768·1024·1440 육안 |
| 3 Button | 위 + `src/shared/components/__tests__/` |
| 4 훅 | 위 + 해당 도메인 `__tests__/` |
| 5 폴더 | `npm run typecheck` · `npm run check:cycles` |
| 6 분해 | 해당 도메인 테스트 |
| 7 | `npm test` · `npm run test:e2e` |

**e2e 주의 2건:**

- `develop`에서도 선행 파손이 있다. 실패를 이 브랜치 탓으로 단정하지 말고 `develop`에서 같은
  명령을 한 번 돌려 비교한다.
- 워크트리 병렬 실행 시 **포트 4306이 충돌**한다. 다른 워크트리의 e2e가 돌고 있으면 실행하지 않는다.
- 병렬 워커(`--workers=2` 이상)에서 결과가 흔들린다 — 실패 목록은 `--workers=1`로 재확인한다.

---

## 6. 병합 시 주의

`refactor/214-ui-redesign` 워크트리에 **미커밋 UI 리디자인 작업물이 15개 파일 이상** 있다
(`Arena` · `EntryPage` · `PingPongGame` · `PlayModeDialog` · `DuelGame` …). 이 티켓과 대상 파일이
거의 겹친다. `develop`에 먼저 들어가는 쪽이 나중 쪽의 충돌 비용을 만든다 —
**214를 먼저 마무리해 병합하고 215가 그 위에서 리베이스하는 순서를 권한다.**

---

## 7. 진행 기록

| Phase | 상태 | 커밋 | 비고 |
|---|---|---|---|
| 0 기준 확정 | 완료 | — | 이 문서 |
| 1a 흰색 알파 사다리 | **완료** | 미커밋 | 토큰 3개 신설, 16파일 26곳 치환. 외관 변화 0 |
| 1b duel/pingpong 크롬 | 대기 | | three.js 짝 주의 |
| 2 화면 셸 | 대기 | | |
| 3 Button 수렴 | 대기 | | |
| 4 훅·유틸 분리 | 대기 | | |
| 5 폴더 구조 | 대기 | | import 이동만 |
| 6 컴포넌트 분해 | 대기 | | |
| 7 마무리 | 대기 | | 문서 반영 |

---

## 8. 결정 기록

```text
- 날짜: 2026-08-05
- 변경한 결정: shadcn/ui를 도입하지 않는다
- 근거: 대체 대상 788줄 + 테스트 711줄이 이 저장소에서 중복이 가장 적고 테스트가 가장 촘촘한
  영역이다. Button은 63줄 → shadcn 등가물 55줄로 이득이 8줄이다. 반면 --ds-* 토큰 체계와
  cn.ts의 tailwind-merge 충돌 그룹, AnimatePresence 기반 퇴장 애니메이션을 모두 다시 짜야 한다.
  실제 중복은 화면 셸 25곳·생 button 68곳·200줄 초과 31개인데 shadcn은 그중 아무것도 건드리지 못한다.
- 영향: 새 의존성 0개로 진행한다. Screen 셸 + Button 수렴 + 분해가 대안이다.
- 후속 작업: Popover의 scroll 미추적 버그는 @floating-ui/react 단독 도입으로 별개 티켓 처리
```

```text
- 날짜: 2026-08-05
- 변경한 결정: 「주석 적게」를 "what은 지우고 why만 3줄 이내로"로 구체화했다
- 근거: 현재 주석 중 상당수가 실제 디버깅으로 얻은 결론이다(recipes.css의 @layer 선택,
  cn.ts의 extendTailwindMerge 필요성, h-svh vs min-h-svh). 통째로 지우면 같은 함정을 다시 밟는다.
  이전 티켓 .dev.md의 결정 기록이 그 증거다.
- 영향: 양은 줄이되 20줄 블록 주석의 근거는 docs/로 이동한다(Phase 7).
- 후속 작업: 없음. 이 판단을 뒤집으려면 기준 1.5를 고친다.
```

```text
- 날짜: 2026-08-05
- 변경한 결정: 컴포넌트당 폴더 콜로케이션을 채택하지 않고 도메인 우선 구조를 유지한다
- 근거: 일반 프론트엔드 가이드는 components/Foo/Foo.tsx를 권하지만, 이 저장소는 도메인 우선을
  택한 이유가 CLAUDE.md에 기록되어 있다 — "레이어를 먼저 두면 야추 하나를 이해하려고 6개 폴더를
  동시에 뒤져야 했다". 게임이 3개(yacht·duel·pingpong)로 늘어난 지금 그 판단은 더 맞다.
- 영향: Phase 5는 duel/·pingpong/을 yacht/와 같은 도메인 내 레이어 구조로 맞추는 일이 된다.
- 후속 작업: 한 컴포넌트가 파일 4개 이상으로 갈라지면 그때 폴더를 만든다
```

```text
- 날짜: 2026-08-05
- 변경한 결정: Phase 1(토큰)을 Phase 2(셸)보다 앞에 둔다
- 근거: 셸 25곳의 문자열이 갈라진 주된 이유가 도메인별 원시 hex(#0b0409 듀얼 · #070b12 탁구)와
  text-white 53곳이다. 토큰화 전에 셸을 공통화하면 배경색을 props로 받는 잘못된 API가 나온다.
- 영향: 첫 커밋이 셸이 아니라 토큰이 된다. 눈에 보이는 변화가 없는 커밋이라 리뷰 시 설명이 필요하다.
- 후속 작업: 없음
```
