# Local planning and reference materials
/.dev.md

# Build output — 루트 .gitignore에도 있지만, Tailwind v4 소스 탐지는
# frontend/ 기준으로 .gitignore를 읽으므로 여기에도 있어야 이전 빌드 산출물을 스캔하지 않는다.
/dist/

# CodeGraph index
/.codegraph/

# 족보 콜아웃 녹음 원본. 저장소에는 변환 결과(public/audio/hand-voice/*.wav)만 올린다 —
# 원본 테이크는 용량이 크고 재변환은 scripts/import-hand-voice.mjs로 언제든 다시 된다.
/scripts/voice-source/*
!/scripts/voice-source/README.md
