# 코드 품질 게이트 도입 계획

목표로 제시된 기준은 다음 11개입니다.

| # | 지표 | 목표 | 지금 | 게이트 |
|---|---|---|---|---|
| 1 | 사이클로매틱 복잡도 | < 22 | 최대 21 | 켜짐 |
| 2 | 인지 복잡도 | < 22 | 진단 0건 | 켜짐 |
| 3 | 할스테드 난이도 | < 80 | 최대 74.8 | 켜짐 |
| 4 | 파일당 코드 라인 수 | < 500 | 최대 471 | 켜짐 |
| 5 | 테스트 커버리지 | 100% | 핵심 모듈에서 진행 중 | 래칫 |
| 6 | CRAP | < 25 | 핵심 최대 21.0 | 켜짐(핵심) |
| 7 | 생존 돌연변이 | 0 | 생존 0 · 사멸 1,263 | 켜짐(야간) |
| 8 | 죽은 코드 | 0 | 0건 | 켜짐 |
| 9 | 중복 코드 | 0 | 0쌍 | 켜짐 |
| 10 | `any` 타입 | 0 | 0건 | 켜짐 |
| 11 | `unknown` 타입 | 0 | 58건 | 래칫 |

`npm run quality`가 이 표를 그대로 출력합니다. 아래 5절이 지표마다 무엇을 했고
무엇이 남았는지 적습니다.

---

## 1. `interlinked-cli` 확인 결과

### 실체

`interlinked-cli`는 실재하는 도구가 맞습니다. 다만 npm 레지스트리에는 등록되어 있지
않고 GitHub 저장소에서 직접 설치해야 합니다.

- 저장소: `github.com/QuentinCody/interlinked-cli` (MIT, 개인 메인테이너 1인)
- 설치: `npm install -g github:QuentinCody/interlinked-cli`
- 요구 사항: Node.js 22 이상, macOS 및 Linux(WSL 포함)
- 정체성: **코딩 에이전트용 훅 하네스**입니다. Claude Code·Codex·Cursor 등의
  도구 호출을 가로채서, 편집 직후에 33개의 품질 검사와 26개의 구조 검사를
  실행하고 필요하면 편집 자체를 차단합니다. 로컬 유닉스 소켓 데몬으로 동작하며
  기본적으로 외부로 데이터를 전송하지 않습니다.
- 강제 수준은 `interlinked mode`로 `lenient`·`balanced`(기본)·`strict` 중에서
  선택합니다. 취향(taste) 규칙은 `strict`에서만 편집을 실제로 막습니다.

### 목표 지표를 실제로 얼마나 담당하는가

| 지표 | interlinked-cli 지원 여부 | 근거 |
|---|---|---|
| `any` / `unknown` | **직접 지원** | `strong_typing` 검사(경고)가 명시적 `any`와 `unknown`을 탐지하고, `strict_typing_block` 검사(오류)가 `as any`·`as unknown as`·`@ts-ignore`·`: any`를 편집 단계에서 차단합니다. 후자는 기본 비활성이며 `guard-rules.local.json`에서 켭니다. |
| 죽은 코드 | **부분 지원** | 구조 검사에 `dead_exports`(임포트하는 곳이 없는 export)와 `dead_imports`(사용되지 않는 import)가 있습니다. 다만 도달 불가능한 파일 전체나 미사용 의존성은 다루지 않습니다. |
| 테스트 커버리지 | **배관만 지원** | `interlinked coverage`는 커버리지를 측정하지 않습니다. 사용자가 만든 `coverage-summary.json`을 읽어 파일별로 **하락을 막는 래칫**으로 동작할 뿐이며, 100%라는 절대 기준을 강제하는 기능이 아닙니다. |
| 생존 돌연변이 | **배관만 지원** | `interlinked mutation` 역시 Stryker 리포트를 사용자가 생성해 두어야 하고, 그 점수의 하락을 막는 래칫입니다. |
| 사이클로매틱 복잡도 | **미지원** | 검사 목록에 없습니다. |
| 인지 복잡도 | **미지원(간접)** | 자체 계산은 없고, `biome_lint` 검사가 프로젝트의 Biome 설정을 그대로 실행할 뿐입니다. 즉 인지 복잡도는 우리 Biome 설정이 담당합니다. |
| 할스테드 난이도 | **미지원** | |
| 파일당 라인 수 | **미지원** | |
| CRAP | **미지원** | |
| 중복 코드 | **미지원** | 동일 export 이름을 찾는 `duplicate_symbols`는 있으나 이것은 코드 복제 탐지가 아닙니다. |

### 결론

**"이걸 쓰면 목표가 달성된다"는 아닙니다.** 11개 지표 가운데 완전히 담당하는 것은
`any`/`unknown` 하나뿐이고, 죽은 코드는 절반, 커버리지와 돌연변이는 다른 도구가
만든 리포트를 소비하는 래칫에 그칩니다. 복잡도 3종·라인 수·CRAP·중복은 아예
다루지 않습니다.

그렇지만 **보완재로서는 의미가 있습니다.** 이 도구의 가치는 지표 측정이 아니라
**측정 시점**에 있습니다. CI는 커밋이 끝난 뒤에 알려 주지만, 이 하네스는 에이전트가
파일을 쓰는 순간에 막아 줍니다. `/enforce` 기능은 `AGENTS.md`·`CLAUDE.md`의 산문
규칙을 결정론적 규칙으로 변환해 주기도 하는데, 상세한 지침 문서를 유지하는 이
저장소의 성격과 잘 맞습니다.

따라서 다음과 같이 역할을 나누는 것을 제안합니다.

- **A 계층(필수·기준점):** npm 스크립트와 GitHub Actions로 구성한 결정론적 게이트.
  실제로 코드베이스를 기준에 맞추는 주체입니다.
- **B 계층(선택·보조):** `interlinked-cli`. A 계층이 만들어 낸 리포트를 소비해서
  에이전트 편집 루프 안에서 즉시 되먹임을 제공합니다. A 계층이 갖추어진 뒤에
  도입하는 편이 순서상 자연스럽습니다.

도입 시 감수해야 할 점도 함께 적어 둡니다. 개인 메인테이너 한 명이 관리하는
비공식 도구를 git URL로 설치하며, 모든 에이전트의 도구 호출을 경유시키는 로컬
데몬을 상시 실행하게 됩니다. README에 적힌 수치보다 `docs/generated/`의 자동 생성
문서를 신뢰해야 한다고 저장소 스스로 안내하고 있습니다.

---

## 2. 0단계 기준선 (실측)

`npm run quality:baseline`이 산출한 값이며 `tools/quality/baseline.json`에 기록되어
있다. 아래 수치는 전부 `backend-java/` 삭제 **이후**에 측정했다.

| 항목 | `backend/` | `frontend/` |
|---|---|---|
| 비테스트 파일 | 160 | 312 |
| 비테스트 코드 라인(주석·빈 줄 제외) | 14,300 | 28,859 |
| 비테스트 전체 라인 | 20,033 | 32,027 |
| 테스트 파일 | 107 | 154 |
| 함수 | 3,494 | 4,800 |
| 커버리지(lines) | 91.93% | 83.77% |

### 11개 지표 현황

| # | 지표 | 목표 | 기준선 | 위반 |
|---|---|---|---|---|
| 1 | 사이클로매틱 복잡도 | < 22 | 최대 24 | 1 |
| 2 | 인지 복잡도 | < 22 | Biome 진단 0건(기본 상한 15에서도 없다) | 0 |
| 3 | 할스테드 난이도 | < 80 | 최대 84.82 | 1 |
| 4 | 파일당 코드 라인 | < 500 | 최대 1,133 | 11 |
| 5 | 테스트 커버리지 | 100% | backend 91.93 · frontend 83.77 | 2 |
| 6 | CRAP | < 25 | 최대 306 | 33 |
| 7 | 생존 돌연변이 | 0 | 미측정(5단계에서 도입) | — |
| 8 | 죽은 코드 | 0 | 미사용 export 592 · 의존성 2 | 594 |
| 9 | 중복 코드 | 0 | 209쌍 · 2.32% | 209 |
| 10 | `any` 타입 | 0 | 0건 | 0 |
| 11 | `unknown` 이완 | 0 | `as unknown as` 67 · 억제 주석 14 | 67 |

위반 목록의 상세(어떤 파일·어떤 함수인지)는 기준선 파일에 들어 있다.

### 이 문서가 처음에 적었던 수치 가운데 사실과 달랐던 것

**`any` 351건 — 실제로는 0건이다.** `backend/` 219건·`frontend/` 132건이라고 적었으나
`: any`·`as any`·`<any>`·`any[]` 어느 형태도 두 패키지에 존재하지 않는다. 이 문서를
쓴 커밋(`8b376b2`) 시점으로 되돌려 세어 보아도 0이다. 소스에 등장하는 `any`는 전부
`expect.any(Function)` 같은 Vitest 매처다. 따라서 3단계에서 남는 일은
`noExplicitAny`를 `error`로 올리는 설정 변경뿐이고, 실제로 손이 가는 대상은
`as unknown as` 67건이다.

**`frontend/`의 96~98%는 실측이 아니라 설정값이었다.** 그 숫자는
`vitest.config.ts`의 `thresholds`이고, 실측은 statements 82.37 · branches 77.30 ·
functions 84.84 · lines 83.77이다. 즉 **이 임계값은 지금 통과하지 못한다.**
그런데도 CI가 초록인 이유는 `npm test`가 `--coverage` 없이 돌기 때문이다 — 임계값이
설정되어 있을 뿐 한 번도 강제된 적이 없다. 4단계는 "래칫을 새로 만드는 일"이 아니라
**죽어 있는 게이트를 살리는 일**부터 시작한다.

**500줄 초과 파일은 세는 기준에 따라 6개도 되고 4개도 된다.** 전체 라인으로 세면
비테스트 6개, 주석과 빈 줄을 뺀 코드 라인으로 세면 4개다(`handler.ts`는 681줄 가운데
134줄이 주석이다). 지표 이름이 "코드 라인 수"이므로 **코드 라인을 기준으로 삼는다.**
주석을 이유로 파일을 쪼개게 만들면 이 저장소가 유지해 온 설명 주석을 벌주는 셈이 된다.

## 3. 확정된 범위 결정

착수 전에 정해야 했던 사항들이며, 모두 결정되었습니다.

### (a) `backend-java/` — **삭제 완료**

Node 백엔드로의 이식이 끝나 참조 구현으로서의 역할이 사라졌으므로 디렉터리를
통째로 제거했습니다. 따라서 이 문서의 모든 기준은 `backend/`와 `frontend/`
두 TypeScript 패키지에만 적용됩니다. Java 전용 도구 체계(JaCoCo·PIT·Checkstyle·CPD)를
세울 필요가 없어졌습니다.

### (b) 테스트 코드 — **라인 수 기준만 적용**

테스트 파일에는 500줄 기준만 적용해 8개 파일을 분할하고, 복잡도 3종과 CRAP은
적용하지 않습니다. 복잡도를 테스트에 강제하면 표 기반 테스트를 쓰기 어려워져서
오히려 테스트 품질이 떨어집니다. 커버리지와 돌연변이는 애초에 프로덕션 코드를
대상으로 하는 지표입니다.

### (c) 커버리지 100%와 생존 돌연변이 0 — **핵심은 절대 기준, 나머지는 래칫**

- **절대 기준을 적용하는 대상(핵심 순수 로직):** 게임 규칙·점수 계산·상태 전이 등
  외부 의존이 없는 모듈. 여기서는 100%와 0을 CI가 무조건 강제합니다.
  구체적으로는 `backend/src/game/**`의 규칙·계산 모듈(`*Rules.ts`·
  `*Calculator.ts`·`score*`·`round*`·`ranking/**`)과 `frontend/src/**/model/**`가
  1차 대상입니다.
- **래칫을 적용하는 대상(그 외 전부):** 배선·인프라·렌더링 계층. 현재 수치를
  바닥으로 고정해 하락만 막고, 단계적으로 바닥을 올립니다.
- **예외 관리:** 예외는 반드시 설정 파일에 파일 단위로 명시하고, **예외 목록의
  길이 자체를 CI가 감시**해 늘어나지 못하게 합니다. 이렇게 하면 기준을 낮추지
  않으면서도 진행이 멈추지 않습니다.

### (d) "중복 코드 0"의 정의

문자 그대로의 0은 달성할 수 없습니다. 임포트 블록이나 타입 선언처럼 반복될 수밖에
없는 구조가 항상 존재하기 때문입니다. `jscpd`의 최소 토큰 수를 정해서(예: 50토큰
이상 동일 시 중복) **그 정의 아래에서 0건**을 목표로 삼습니다.

---

## 4. 도구 선정

| 지표 | 채택 도구 | 비고 |
|---|---|---|
| 인지 복잡도 | Biome `complexity/noExcessiveCognitiveComplexity` | 이미 설치되어 있으며 `warn`에서 `error`로 올리고 상한 옵션을 22로 설정합니다. 옵션 키 이름은 도입 시 Biome 2.5 문서에서 확인합니다. |
| 사이클로매틱 복잡도 | 자체 분석기 (`tools/quality/`) | Biome에는 해당 규칙이 없습니다. ESLint 코어의 `complexity` 규칙을 쓰려면 ESLint 체계를 새로 들여와야 하는데, Biome을 쓰는 저장소에 린터를 이중으로 두는 비용이 큽니다. |
| 할스테드 난이도 | 자체 분석기 (동일) | `ts-complex`(2018)·`typhonjs-escomplex`(2018)·`codehawk-cli`(2023)는 모두 유지 보수가 멈추었고 최신 TS 문법 지원이 불확실합니다. |
| CRAP | 자체 분석기 + Vitest 커버리지 리포트 | 함수별 사이클로매틱 복잡도와 함수별 커버리지를 결합해 계산합니다. |
| 파일당 라인 수 | 자체 분석기 (동일) | |
| 테스트 커버리지 | Vitest + `@vitest/coverage-v8` | `frontend/`에 이미 있고 `backend/`에 새로 추가합니다. |
| 생존 돌연변이 | `@stryker-mutator/core` 10 + `@stryker-mutator/vitest-runner` | |
| 죽은 코드 | `knip` 6 | 미사용 파일·export·의존성을 한 번에 다룹니다. `ts-prune`은 2021년 이후 정지 상태입니다. |
| 중복 코드 | `jscpd` 5 | |
| `any` / `unknown` | Biome `suspicious/noExplicitAny`(error) + 자체 스캔 | `as unknown as`와 `biome-ignore` 억제는 Biome 규칙으로 잡히지 않으므로 별도 스캔이 필요합니다. |

### 자체 분석기에 관하여

`tools/quality/analyze.mjs`가 파일을 순회하며 함수별로 사이클로매틱 복잡도·할스테드
지표·라인 수를 계산하고, Vitest가 만든 `coverage-final.json`의 statement 실행 정보와
결합해 CRAP까지 산출합니다. `tools/quality/report.mjs`는 그 결과에 Biome·knip·jscpd·
커버리지 요약을 합쳐 11개 지표를 한 표로 만들고, `--gate`에서 **켜져 있는 게이트만**
검사합니다. 어떤 게이트가 켜져 있는지는 `tools/quality/config.json`의 `enforced`가
정합니다 — 단계적으로 켜는 것이 계획이므로 그 스위치가 코드가 아니라 설정에 있어야
합니다.

인지 복잡도는 이 분석기가 계산하지 않습니다. Biome이 이미 그 지표를 담당하므로
같은 값을 두 번 정의하지 않고, 리포터가 Biome 진단을 수집합니다.

**파서로 저장소의 `typescript` 의존성을 쓰지 못했습니다.** 이 저장소는 TypeScript 7을
쓰는데 7.x는 Go 네이티브 포팅이라 JS 컴파일러 API(`ts.createSourceFile`·`ts.SyntaxKind`)를
내보내지 않습니다. 그래서 분석 전용으로 `typescript@5.9`를 루트 `package.json`에 따로
둡니다. TypeScript 7은 5.9와 문법 집합이 같으므로 파싱 결과에는 차이가 없습니다.

여기서 짚어 둘 점이 하나 있습니다. **CRAP은 커버리지가 100%일 때 사이클로매틱
복잡도와 같아집니다.** 정의가 `복잡도² × (1 − 커버리지)³ + 복잡도`이므로 커버리지가
1이면 뒤쪽 항이 사라지기 때문입니다. 즉 지표 5번과 1번을 달성하면 6번은 자동으로
따라옵니다. CRAP 기준이 실제로 의미를 갖는 구간은 커버리지를 100%로 끌어올리는
과도기뿐이며, 그 기간 동안 "복잡한데 테스트되지 않은 함수"를 우선순위 순으로
드러내 주는 역할을 합니다.

---

## 5. 단계별 실행 계획

### 0단계 — 계측 (게이트 없음) — **완료**

1. ~~`tools/quality/` 분석기를 작성합니다.~~ `analyze.mjs`·`report.mjs`·`config.json`.
2. ~~`backend/`에 `@vitest/coverage-v8`을 추가하고 커버리지 설정을 넣습니다.~~
3. ~~`knip`·`jscpd`를 추가하고 설정 파일을 작성합니다.~~ 루트 `package.json`에 두었습니다 —
   두 도구 모두 두 패키지를 가로질러 봐야 하기 때문입니다.
4. ~~11개 지표의 현재 수치를 산출해 기록합니다.~~ 2절과 `tools/quality/baseline.json`.
5. ~~이 기준선을 보고 3절의 결정 사항을 확정합니다.~~

기준선 파일을 `docs/`가 아니라 `tools/quality/`에 둔 이유: 루트 `.gitignore`가
`/docs/`를 통째로 무시합니다(로컬 기획 자료 보관용). 래칫의 바닥이 되려면 커밋되어야
하므로 도구와 같은 자리에 둡니다.

CI는 `backend.yml`·`frontend.yml`이 아니라 `.github/workflows/quality.yml` 하나에
배선했습니다. 분석기와 knip·jscpd는 두 패키지를 한 번에 읽어야 하는데(중복은 패키지를
가로질러 생기고, knip은 워크스페이스 전체를 봐야 미사용 export를 판정합니다), 잡을
`backend.yml`에 넣으면 프론트만 바뀐 PR에서 돌지 않고 양쪽에 넣으면 같은 분석을 두 번
돌리게 됩니다.

명령은 다음과 같습니다.

```bash
npm run quality            # 측정하고 표를 출력한다(커버리지는 기존 리포트를 읽는다)
npm run quality:coverage   # 커버리지까지 새로 측정한다
npm run quality:baseline   # 측정하고 기준선 파일을 갱신한다
npm run quality:gate       # 켜진 게이트를 검사한다 — CI가 부르는 명령
```

### 1단계 — 즉시 강제 가능한 게이트 — **완료**

- **파일당 500줄 — 게이트 켬.** 코드 라인 기준으로 위반이 11개였고 지금은 예외 하나만
  남았다. 뒤늦게 507줄이 된 `davinciRules.test.ts`는 잘못 불린 전이를 물리치는 갈래를
  따로 떼고 공통 준비를 픽스처로 모아 갈랐다 — 지금 가장 긴 파일은 471줄이다. 프로덕션은 `ws/handler.ts`(입장 흐름 분리)·`pingpong/scene3d.ts`(텍스처·재료·
  라켓·마스코트·무대·배우 분리)·`landing/heroScene.ts`(소품·배색 분리)를 갈랐고, 테스트는
  픽스처를 하네스로 빼고 검사를 주제별로 나누는 같은 방법을 다섯 스위트에 적용했다.

  예외는 `physics-dice/World.ts`(760줄) 하나다. 굴림 단계 진행(흔들기→기울이기→방출→
  궤적 재생→결과 정렬)이 스무 개 남짓한 필드를 함께 읽고 쓰는 상태 기계라, 파일을 가르는
  일이 곧 그 상태의 재설계다. 겉모습이 바뀌지 않았음을 단위 테스트만으로는 다 보이지
  못하므로 시각 대조를 함께 세운 뒤에 손대는 것이 옳다.

- **죽은 코드 — 게이트 켬.** 592건을 걷어냈고 지금 0이다. 계약 파일 두 개
  (`ws/protocol.ts`·`realtime/wsEvents.ts`)만 예외이며 근거는 `config.json`에 있다.

- **중복 코드 — 게이트 켬.** 209쌍(2.32%)에서 0쌍(0.05%)이 되었다. 세 갈래로 처리했다.

  **첫째, 실제로 겹치던 것은 한 자리로 모았다.** 프로덕션 쪽에서 가장 나빴던 것은
  **검사가 프로덕션 로직을 베낀 자리**다 — `physicsSimulation.test.ts`가 주사위를
  그릇에 앉히고 쏟아내는 두 함수를 통째로 복사해 두고 있었다. 복사본이 낡으면 검사가
  실제 화면과 다른 것을 재게 되므로 프로덕션 함수를 부르도록 바꿨다. 그 밖에 족보
  판정과 봇 전략의 개수 세기(`diceCounts`), 회원 행을 잠그고 읽는 절차
  (`lockUserRow`), 4xx를 빈 본문으로 내보내는 오류 핸들러(`sendEmptyClientErrors`),
  점수판 통지 봉투(`scoreUpdateEnvelope`), 서버 봉투 빌더(`buildServerMessage`)를
  각각 한 자리로 모았다. 검사 쪽은 라우터 대역·키 입력·기기 흔들림·미디어 질의·
  브로드캐스터 기록기·게임 모듈 대역·소켓 클라이언트 같은 준비 절차를 공유 하네스로
  뽑았다.

  **둘째, 지표의 정의를 다듬었다.** import 문만으로 이루어진 조각은 세지 않는다.
  이 지표가 막으려는 사고는 같은 규칙이 두 자리에 적혀 한쪽만 고쳐지는 것인데,
  import 목록은 그 위험이 아니다 — 무엇을 쓰는지가 겹칠 뿐이고 어긋나면 컴파일러가
  먼저 잡는다.

  **셋째, 남은 세 쌍은 예외로 명시했다**(상한을 5에서 8로 올렸다). 다빈치·결투
  모듈의 배선은 ADR-0007대로 게임마다 자기 것을 갖는 편이 낫고(겹치는 것은 규칙이
  아니라 세 줄짜리 배선이며 두 게임의 서비스 타입이 서로 다르다), 포트 계약 검사는
  게임마다 자기 타입으로 대입해야 뜻이 있으며, 물리 검사의 흔들기 루프는 화면 함수로
  갈아 끼우면 난수 소비 순서가 달라져 24개 seed 표본이 통째로 바뀐다. 근거는 셋 다
  `config.json`에 적었다.

  처음에 "게임 모듈이 같은 뼈대를 복제한다"고 본 45쌍은 실제로는 `SocketGameModule`이
  이미 걷어낸 뒤였고, 남은 것은 위의 배선 한 쌍뿐이었다.

- **CI 배선 — 완료.** `.github/workflows/quality.yml`.

### 2단계 — 복잡도 3종 — **완료**

세 지표 모두 게이트를 켰다. 위반은 둘뿐이었다.

- `RealtimeSync`의 `applyServerMessage`가 한 switch에 24개 case를 담아 사이클로매틱
  복잡도가 그대로 24였다. 갈래가 실제로 셋(세션·방·게임)이라 그렇게 나눴다.
- `pingpong`의 `createScene`이 텍스처·재료·무대·배우를 한 함수에서 다 만들고 있었다.
  무대와 배우를 각각 함수로 세우자 할스테드 난이도가 84.8 → 74.8로 내려갔다.

인지 복잡도는 기본 상한(15)에서도 위반이 0이었다. Biome 규칙을 `error`·상한 22로
올렸고, 옵션 키가 실제로 먹는지는 음성 대조로 확인했다 — 상한을 1로 낮추면 20건이
잡히고 22로 되돌리면 0건이다.

### 3단계 — 타입 이완 제거 — **완료(나머지는 래칫)**

`any`는 애초에 0건이었으므로 Biome의 `noExplicitAny`는 켜 두는 것으로 끝났다
(recommended 프리셋에 이미 error로 들어 있다). 실제 작업은 `as unknown as` 쪽이었고
67건에서 57건으로 줄였다.

**없앤 8건.** 전부 "타입을 속일 필요가 없는데 속이고 있던" 자리다.

- `infra/lua.ts` — 동적으로 붙는 Lua 명령을 찾으려고 Redis를 인덱스 시그니처로
  바꿔치던 것을 `Reflect.get`으로 바꿨다. 그러면 캐스트 없이 `unknown`이 나온다.
- `ws/gateway.ts` — `ClientSocket`은 `ws`의 소켓이 그대로 만족하는 최소 표면이라
  이중 캐스트가 필요 없었다(그 파일 주석이 이미 그렇게 말하고 있었다).
- 배열을 5칸 튜플로 만들던 네 자리(`dice.ts`·`remap.ts`·`tutorialGame.ts`·
  `realtimeScenarios.ts`) — `map`은 길이를 모르는 배열을 주므로 캐스트가 필요해졌던
  것이다. 자리마다 부르면 튜플이 그대로 만들어진다.
- `useLobbyRoom.ts`·`MotionInputController.ts` — 구형 브라우저에 없는 API 하나를
  확인하려고 `window`와 `DeviceMotionEvent` 전체를 다른 타입으로 바꿔치고 있었다.
  옵셔널 속성으로 받으면 된다.

**남은 57건과 래칫.** 성격이 둘로 갈린다.

- **테스트 스텁(45건).** 부분 구현 객체를 인터페이스 자리에 넣는 자리다. 완전한
  스텁을 만들게 하면 테스트가 무엇을 검증하는지 오히려 흐려지므로 정당한 자리로 본다.
- **게임 상태 캐스트(약 7건).** `snapshot.game`의 타입이 야추 모양(`GameState`)
  하나뿐이라, 다빈치·결투·탁구 화면이 자기 뷰 타입으로 바꿔 읽는다. 근본 해결은
  `game`을 게임별 유니온으로 만들고 `gameCode`로 좁히는 것인데, 그것은 와이어 계약
  정본(`realtime/wsEvents.ts`)의 재설계이고 `RealtimeSync.tsx` 상단 주석이 밝히듯
  **이관 티켓이 그 파일을 통째로 다시 가른다.** 그때 함께 간다.

미디어 질의 대역을 공유 하네스로 뽑으면서 `as unknown as MediaQueryList` 세 건이
함께 사라졌다. 그래서 이 지표는 절대 기준 대신 **래칫**으로 잡았다(`config.json`의
`ratchets`).
57을 바닥으로 고정하고, 줄어들면 상한도 함께 줄인다 — 리포터가 여유가 생길 때마다
"조일 수 있다"고 알려 준다.

### 4단계 — 커버리지 — **래칫·핵심 CRAP 게이트 완료, 100%는 남은 자리가 정해졌다**

**먼저 한 일은 죽어 있던 게이트를 살리는 것이었다.** `frontend`의 임계값 96·91·96·98은
실측이 아니라 목표였고, `npm test`가 `--coverage` 없이 도는 바람에 CI에서 한 번도
강제된 적이 없었다. 지금은 실측을 바닥으로 고정했다.

| 패키지 | 처음 | 지금 |
|---|---|---|
| `backend/` | 임계값 없음 | **92·85·91·93** |
| `frontend/` | 96·91·96·98(허수) | **87·82·89·88** |

두 패키지의 `npm test`가 커버리지를 함께 재므로 수치가 내려가면 CI가 막는다. 테스트
하네스는 집계에서 뺐다 — 검사를 돕는 도구지 측정 대상이 아니다. 검사를 붙일 때마다
바닥도 함께 올렸다.

**핵심 순수 로직 100%는 남은 자리가 열 개까지 줄었다.** 처음 26개 파일 120문이던
미달이 **10개 파일 14문**이다. 백엔드는 결투·다빈치·탁구의 점수 기록 어댑터를
`score/rosterScoreWriter.ts` 하나로 모으고 진짜 Redis로 검사를 붙였고, 프론트는
도메인을 먼저 채운 뒤 모델 훅으로 넘어갔다 — 실시간 클라이언트 대역과 가짜 타이머로
프레임 루프를 세우고, 3D 월드는 적재 시점까지 손에 쥐는 대역으로 바꿔 끼웠다.

**남은 14문의 성격은 둘뿐이다.**

- **MySQL을 타는 저장소(5문).** `weeklyRankingStore`는 `MYSQL_TEST_URL`이 없는 환경에서
  통합 검사가 skip되므로 로컬에서만 낮게 나온다. CI의 `backend.yml`이 덮는다.
- **타입이 이미 막고 있는 런타임 방어(9문).** `scoreBoard`·`scoreBoardStore`·
  `scoreConfirmationService`·`readScoreBoard`의 `null` 검사, `usePartyHostGame`의
  프레임 루프 방어, `tutorialGame`의 순열 검사 같은 자리다. 여기 닿으려면 검사가
  타입을 속여야 하는데, 그것은 `unknown` 이완을 0으로 유지한다는 다른 기준과
  부딪힌다. 덮지 않는 편이 옳다고 판단했고, 각 파일에 그 사정을 주석으로 남겼다.

**CRAP의 적용 범위도 3절 (c)를 따른다.** 절대 기준(< 25)은 핵심 순수 로직에만 건다
(`config.json`의 `coreModules`). 전 범위에 걸면 위반 32건 가운데 스물 몇 건이 결투 화면
컴포넌트인데, 그것을 0으로 만들려면 프론트가 금지한 "단순 렌더링 확인용 검사"를 찍어내야
한다(frontend/AGENTS.md 「테스트 최소화 원칙」). 두 문서가 부딪히는 자리라 계획서가 이미
정해 둔 갈래를 따랐다.

**핵심 범위의 CRAP 위반은 0이 되었고 게이트를 켰다.** 7건 → 5건 → 0건이다. 남아 있던
다섯은 전부 React 훅(`useDuelGame`·`useLocalPingPongGame`·`usePartyHostGame`)이었고,
실시간 클라이언트 대역과 가짜 타이머로 프레임 루프를 세워 검사를 붙였다. 핵심 최대
CRAP은 30에서 21.01로 내려갔다.

- 결투: 뽑기의 세 갈래(조기 발사·정상 발사·이미 반응함)와 피격 시점 계산을 덮었다.
- 탁구: 3D 무대를 대역으로 바꾸고 rAF를 가짜 타이머로 돌려, 점수판 갱신 조건·입력
  분기·전적 보고·파티 판정 보고 주기를 덮었다.
- 그 뒤로 `usePingPongGame`·`useHeroCarousel`·`usePhysicsDiceWorld`·`useDavinciGame`·
  `useReactionDock`·`useLobbyActions`·`useLobbyChrome`·`useRollFeedback`·`useSpotlight`·
  `useAuthSessionCheck`·`useGamePlayRoll`·`useGamePlaySubmission`·`useSecondsLeft`·
  `useLocalRoomSnapshot`까지 열넷을 더 덮었다.

**게이트의 입력이 없으면 통과시키지 않는다.** CRAP은 커버리지가 있어야 판정되는데,
품질 워크플로는 커버리지를 재지 않고 있었다. 그대로 켜면 측정값이 없어 위반 0으로
보이는 "통과할 수밖에 없는 게이트"가 된다. 그래서 두 가지를 함께 바꿨다 —
`report.mjs`의 `GATE_INPUTS`가 켜진 게이트의 측정값이 없으면 실패로 멈추고, 품질
워크플로의 게이트 잡이 두 패키지의 테스트를 커버리지와 함께 돌린다(MySQL 통합 테스트는
`backend.yml`이 맡는다 — 핵심 모듈은 MySQL을 타지 않으므로 판정이 달라지지 않는다).

### 5단계 — 생존 돌연변이 — **범위 안에서 0 달성, 게이트 켜짐**

Stryker 10 + vitest-runner를 `backend/`에 도입했다. 첫 실행은 순수 로직 모듈 6개에서
**생존 45 · 사멸 350**이었고, 지금은 **12개 파일에서 생존 0 · 사멸 1,263**이다.
게임 규칙 3종(결투·다빈치·탁구)이 모두 들어왔다.

CI는 야간 스케줄과 수동 실행에서만 돈다. PR마다 돌리면 리뷰가 그만큼 늦어진다. 생존 0을
강제하는 자리도 **stryker를 돌린 그 잡 안**이다(`tools/quality/mutation-gate.mjs`).
리포트는 커밋되지 않으므로 PR 게이트에는 없고, 없는 것을 0으로 읽으면 게이트가 아니라
장식이 되기 때문이다.

**설정에서 짚어 둘 것 하나.** `tsconfigFile`을 빈 값으로 두었다. Stryker는 샌드박스에
tsconfig를 옮기면서 `ts.parseConfigFileTextToJson`을 부르는데, 이 저장소의 TypeScript 7은
그 JS API를 내보내지 않아 그대로 두면 실행 자체가 실패한다. 전처리를 건너뛰어도 돌연변이
주입과 실행에는 영향이 없다.

**돌연변이 검사가 실제로 결함을 드러냈다.** 동점일 때 playerId 오름차순으로 줄을 세우는
규칙에 검사가 없어 결과 화면의 순서가 사람마다 뒤집힐 수 있었고, 합계의 세 자리 음수
검사가 하나로 뭉쳐 있었으며, 값이 없는 칸을 거르지 않아 소계가 `NaN`이 되는 자리가 있었다.
"실행은 되지만 값이 바뀌어도 아무 검사가 실패하지 않는" 자리를 찾는 데 이 도구가 값을
한다는 6절의 평가는 그대로 유효하다.

**게임 규칙 3종을 넓히며 걸린 것은 매번 같은 성격이었다.** 결투 30건·다빈치 46건·
탁구 114건이었고, 셋 다 아래 셋으로 갈렸다.

- **시각과 값이 계약인 자리.** 세 게임 모두 서버가 예약한 마감 하나로 굴러가는데
  (`nextActionAt`), 검사가 상태 전이만 보고 그 값을 보지 않았다. 결투의 신호·유예·
  연출 길이, 다빈치의 국면별 마감(추측 30초·결정 15초·놓기 15초), 탁구의 비행
  시간이 그렇다. 특히 탁구는 서버가 프레임을 보내지 않고 클라이언트가
  `pos + direction × speed × 경과초`로 같은 공을 그리므로 **좌표계 자체가 계약**이다.
- **국면과 상태가 어긋난 프레임.** 상태는 Redis에서 JSON으로 되돌아오고 서비스가
  예약을 잘못 걸 수도 있다. 결과가 남아 있는데 국면이 결과가 아닌 프레임, 명단
  자리가 빈 프레임, 손패가 통째로 빠진 사람이 실제로 들어온다.
- **조건을 한 번에 넘겨 짚기.** 가드가 다섯 조건을 `||`로 잇고 있는데 검사가 그
  다섯을 한 프레임으로 확인하면, 하나가 무너져도 나머지가 가려 준다. 조건마다 그
  조건만 어긋난 프레임을 따로 만들어야 한다.

**동치 돌연변이는 사유와 함께 제외했다.** 죽일 수 없는 것을 죽이려 애쓰면 검사가
구현을 베끼게 된다. 판별한 것들: 같은 값이 이미 앞에서 갈린 뒤의 `<`와 `<=`,
`1 | -1` 타입에서의 `> 0`과 `>= 0`, 홀짝만 쓰는 순번 계산의 더하기와 빼기, 앞뒤
조건이 같은 것을 잡는 타입 좁히기용 검사가 그것이다.

**stryker의 판정을 그대로 믿지 않고 확인했다.** 살아남았다고 보고된 자리를 손으로
바꿔 검사를 돌려 보면 실제로는 검사가 잡는 경우가 있었다. 위치 정보(`location`의
시작·끝 열)를 읽어 **조건 전체가 아니라 피연산자 하나**가 바뀐 것임을 확인하고 나서야
무엇을 덮어야 하는지가 분명해졌다. `coverageAnalysis`를 `all`·`perTest`·`off`로
바꿔 가며 대조한 결과 매핑 문제가 아니었다.

범위 밖(어댑터·화면·인프라)으로 넓히는 것은 다음 일이다. 그쪽은 Redis·MySQL을 타므로
순수 로직과 성격이 다르고, 돌연변이가 검사 시간을 크게 늘린다는 점을 함께 재야 한다
(지금도 12개 파일에 4분이 걸린다).

### 6단계 — `interlinked-cli` 도입(선택)

앞 단계에서 만들어 둔 리포트를 소비하도록 연결합니다.

- `interlinked enable`로 훅과 스킬을 설치합니다.
- `interlinked coverage`와 `interlinked mutation`이 각각 Vitest와 Stryker의
  리포트를 읽도록 설정합니다.
- `strict_typing_block`을 `.interlinked/guard-rules.json`에서 활성화합니다.
- 모드는 `balanced`로 시작해 안정화된 뒤 `strict`로 올립니다.

---

## 6. 규모에 관한 솔직한 평가

비테스트 TypeScript 코드가 약 5만 2천 줄입니다. 전 범위에 100% 커버리지와 생존
돌연변이 0을 적용했다면 몇 주가 아니라 몇 달 단위의 일이 되었을 것이고, 그래서
3절 (c)에서 핵심과 나머지를 갈랐습니다. 특히 5단계는
난도가 4단계보다 훨씬 높습니다. 커버리지 100%는 "모든 줄이 한 번은 실행됨"을
뜻할 뿐이지만, 생존 돌연변이 0은 "모든 줄의 모든 변형이 어떤 테스트든 실패시킴"을
요구하기 때문입니다.

그래서 다음과 같이 접근하기를 권합니다.

- 1~3단계(라인 수·중복·죽은 코드·복잡도·타입)는 **전 범위에 절대 기준으로**
  적용합니다. 위반 건수가 적고 효과가 즉시 나타납니다.
- 4~5단계(커버리지·돌연변이)는 3절 (c)대로 핵심에만 절대 기준을 걸고 나머지는
  래칫으로 둡니다. 특히 5단계는 4단계보다 난도가 훨씬 높다는 점을 감안해 일정을
  잡아야 합니다.

가장 큰 위험은 도구 도입이 아니라 **예외 목록의 침식**입니다. 급할 때 예외를 한 줄
추가하는 것은 언제나 쉽고, 그것이 반복되면 게이트는 이름만 남습니다. 예외 개수를
CI가 세도록 만드는 장치를 0단계에 함께 넣는 이유가 이것입니다.
