# 코드 품질 게이트 도입 계획

목표로 제시된 기준은 다음 11개입니다.

| # | 지표 | 목표 |
|---|---|---|
| 1 | 사이클로매틱 복잡도 | < 22 |
| 2 | 인지 복잡도 | < 22 |
| 3 | 할스테드 난이도 | < 80 |
| 4 | 파일당 코드 라인 수 | < 500 |
| 5 | 테스트 커버리지 | 100% |
| 6 | CRAP | < 25 |
| 7 | 생존 돌연변이 | 0 |
| 8 | 죽은 코드 | 0 |
| 9 | 중복 코드 | 0 |
| 10 | `any` 타입 | 0 |
| 11 | `unknown` 타입 | 0 |

---

## 1. `interlinked-cli` 확인 결과

### 실체

`interlinked-cli`는 실재하는 도구가 맞습니다. 다만 npm 레지스트리에는 등록되어 있지
않고 GitHub 저장소에서 직접 설치해야 합니다.

- 저장소: `github.com/QuentinCody/interlinked-cli` (MIT, 개인 메인테이너 1인)
- 설치: `npm install -g github:QuentinCody/interlinked-cli`
- 요구 사항: Node.js 22 이상, macOS 및 Linux(WSL 포함)
- 정체성: **코딩 에이전트용 훅 하네스**입니다. Claude Code·Codex·Cursor 등의
  도구 호출을 가로채서, 편집 직후에 33개의 품질 검사와 26개의 구조 검사를
  실행하고 필요하면 편집 자체를 차단합니다. 로컬 유닉스 소켓 데몬으로 동작하며
  기본적으로 외부로 데이터를 전송하지 않습니다.
- 강제 수준은 `interlinked mode`로 `lenient`·`balanced`(기본)·`strict` 중에서
  선택합니다. 취향(taste) 규칙은 `strict`에서만 편집을 실제로 막습니다.

### 목표 지표를 실제로 얼마나 담당하는가

| 지표 | interlinked-cli 지원 여부 | 근거 |
|---|---|---|
| `any` / `unknown` | **직접 지원** | `strong_typing` 검사(경고)가 명시적 `any`와 `unknown`을 탐지하고, `strict_typing_block` 검사(오류)가 `as any`·`as unknown as`·`@ts-ignore`·`: any`를 편집 단계에서 차단합니다. 후자는 기본 비활성이며 `guard-rules.local.json`에서 켭니다. |
| 죽은 코드 | **부분 지원** | 구조 검사에 `dead_exports`(임포트하는 곳이 없는 export)와 `dead_imports`(사용되지 않는 import)가 있습니다. 다만 도달 불가능한 파일 전체나 미사용 의존성은 다루지 않습니다. |
| 테스트 커버리지 | **배관만 지원** | `interlinked coverage`는 커버리지를 측정하지 않습니다. 사용자가 만든 `coverage-summary.json`을 읽어 파일별로 **하락을 막는 래칫**으로 동작할 뿐이며, 100%라는 절대 기준을 강제하는 기능이 아닙니다. |
| 생존 돌연변이 | **배관만 지원** | `interlinked mutation` 역시 Stryker 리포트를 사용자가 생성해 두어야 하고, 그 점수의 하락을 막는 래칫입니다. |
| 사이클로매틱 복잡도 | **미지원** | 검사 목록에 없습니다. |
| 인지 복잡도 | **미지원(간접)** | 자체 계산은 없고, `biome_lint` 검사가 프로젝트의 Biome 설정을 그대로 실행할 뿐입니다. 즉 인지 복잡도는 우리 Biome 설정이 담당합니다. |
| 할스테드 난이도 | **미지원** | |
| 파일당 라인 수 | **미지원** | |
| CRAP | **미지원** | |
| 중복 코드 | **미지원** | 동일 export 이름을 찾는 `duplicate_symbols`는 있으나 이것은 코드 복제 탐지가 아닙니다. |

### 결론

**"이걸 쓰면 목표가 달성된다"는 아닙니다.** 11개 지표 가운데 완전히 담당하는 것은
`any`/`unknown` 하나뿐이고, 죽은 코드는 절반, 커버리지와 돌연변이는 다른 도구가
만든 리포트를 소비하는 래칫에 그칩니다. 복잡도 3종·라인 수·CRAP·중복은 아예
다루지 않습니다.

그렇지만 **보완재로서는 의미가 있습니다.** 이 도구의 가치는 지표 측정이 아니라
**측정 시점**에 있습니다. CI는 커밋이 끝난 뒤에 알려 주지만, 이 하네스는 에이전트가
파일을 쓰는 순간에 막아 줍니다. `/enforce` 기능은 `AGENTS.md`·`CLAUDE.md`의 산문
규칙을 결정론적 규칙으로 변환해 주기도 하는데, 상세한 지침 문서를 유지하는 이
저장소의 성격과 잘 맞습니다.

따라서 다음과 같이 역할을 나누는 것을 제안합니다.

- **A 계층(필수·기준점):** npm 스크립트와 GitHub Actions로 구성한 결정론적 게이트.
  실제로 코드베이스를 기준에 맞추는 주체입니다.
- **B 계층(선택·보조):** `interlinked-cli`. A 계층이 만들어 낸 리포트를 소비해서
  에이전트 편집 루프 안에서 즉시 되먹임을 제공합니다. A 계층이 갖추어진 뒤에
  도입하는 편이 순서상 자연스럽습니다.

도입 시 감수해야 할 점도 함께 적어 둡니다. 개인 메인테이너 한 명이 관리하는
비공식 도구를 git URL로 설치하며, 모든 에이전트의 도구 호출을 경유시키는 로컬
데몬을 상시 실행하게 됩니다. README에 적힌 수치보다 `docs/generated/`의 자동 생성
문서를 신뢰해야 한다고 저장소 스스로 안내하고 있습니다.

---

## 2. 현재 코드베이스 실측

| 항목 | `backend/` (Node·TS) | `frontend/` (React·TS) | `backend-java/` (Spring) |
|---|---|---|---|
| 소스 파일 수 | 268(테스트 포함) | 464 | 198(main) + 69(test) |
| 비테스트 코드 라인 | 20,113 | 31,595 | 미측정 |
| 테스트 파일 수 | 103 | 154 | 69 |
| 커버리지 설정 | **없음**(`@vitest/coverage-v8` 미설치) | statements 96·branches 91·functions 96·lines 98 | 없음 |
| 인지 복잡도 규칙 | Biome `noExcessiveCognitiveComplexity`: **warn** | 동일하게 **warn** | 없음 |
| 순환 의존성 검사 | 없음 | `dpdm`으로 검사 | 없음 |
| 돌연변이·중복·죽은 코드 도구 | 없음 | 없음 | 없음 |

### 즉시 드러나는 위반

**500줄 초과 비테스트 파일(7개)**

- `frontend/src/yacht/rendering/physics-dice/World.ts` — 798줄
- `frontend/src/pingpong/rendering/scene3d.ts` — 794줄
- `backend/src/ws/handler.ts` — 681줄
- `backend-java/.../handler/GameWebSocketHandler.java` — 626줄
- `frontend/src/realtime/wsEvents.ts` — 590줄
- `backend/src/server.ts` — 579줄
- `frontend/src/landing/rendering/heroScene.ts` — 576줄

**500줄 초과 테스트 파일(9개)** — 최대 1,145줄(`World.test.ts`). 기준을 테스트에도
적용할지는 아래 3절의 결정 사항입니다.

**타입 이완**

- `: any` · `as any` · `<any>` 계열 패턴 351건(backend 219, frontend 132, 테스트 포함)
- `as unknown as` 67건
- `@ts-ignore` 계열 0건, `biome-ignore` 14건
- 다만 `tsconfig`는 이미 `strict`·`noUncheckedIndexedAccess`·
  `exactOptionalPropertyTypes`가 모두 켜져 있어서 출발점이 나쁘지 않습니다.

**커버리지** — `backend/`에는 커버리지 측정 자체가 없어서 현재 수치를 알 수 없습니다.
`frontend/`는 96~98% 선이며, `World.ts`를 비롯한 렌더링·개발용 화면 여러 개를
측정 대상에서 제외해 둔 상태입니다.

---

## 3. 먼저 결정해야 할 사항

계획의 규모가 갈리는 지점이므로 착수 전에 정해야 합니다.

### (a) `backend-java/`를 범위에 포함하는가

현재 운영 중인 백엔드이지만 `backend/`의 Node 구현으로 마이그레이션이 진행 중입니다.
Java에 같은 기준을 적용하려면 JaCoCo·PIT·Checkstyle·CPD로 이루어진 완전히 별개의
도구 체계를 세워야 하는데, 곧 폐기될 코드에 그만한 비용을 들이는 것은 권하지
않습니다. **동결하고 범위에서 제외하는 쪽을 추천합니다.**

### (b) 테스트 코드에도 같은 기준을 적용하는가

500줄 기준을 테스트에 그대로 적용하면 9개 파일을 분할해야 합니다. 복잡도와
할스테드 난이도까지 테스트에 적용하면 표 기반 테스트를 쓰기 어려워집니다.
**테스트에는 라인 수 기준만 적용하고, 복잡도 3종과 CRAP은 제외하는 쪽을
추천합니다.** 커버리지와 돌연변이는 애초에 프로덕션 코드를 대상으로 하는 지표입니다.

### (c) 렌더링 계층을 어떻게 다루는가

`World.ts`·`scene3d.ts`·`heroScene.ts`는 Three.js와 Rapier 물리 엔진을 배선하는
코드입니다. 여기서 100% 커버리지와 생존 돌연변이 0을 달성하려면 렌더러 전체를
목으로 대체해야 하고, 그렇게 만든 테스트는 실제 결함을 거의 잡지 못합니다.
**명시적으로 목록화한 예외로 두고, 예외 목록은 늘어나지 않도록 래칫을 거는 쪽을
추천합니다.**

### (d) "중복 코드 0"의 정의

문자 그대로의 0은 달성할 수 없습니다. 임포트 블록이나 타입 선언처럼 반복될 수밖에
없는 구조가 항상 존재하기 때문입니다. **`jscpd`의 최소 토큰 수를 정해서(예: 50토큰
이상 동일 시 중복) 그 정의 아래에서 0건을 목표로 삼는 것**이 실행 가능한 해석입니다.

---

## 4. 도구 선정

| 지표 | 채택 도구 | 비고 |
|---|---|---|
| 인지 복잡도 | Biome `complexity/noExcessiveCognitiveComplexity` | 이미 설치되어 있으며 `warn`에서 `error`로 올리고 상한 옵션을 22로 설정합니다. 옵션 키 이름은 도입 시 Biome 2.5 문서에서 확인합니다. |
| 사이클로매틱 복잡도 | 자체 분석기 (TypeScript Compiler API) | Biome에는 해당 규칙이 없습니다. ESLint 코어의 `complexity` 규칙을 쓰려면 ESLint 체계를 새로 들여와야 하는데, Biome을 쓰는 저장소에 린터를 이중으로 두는 비용이 큽니다. |
| 할스테드 난이도 | 자체 분석기 (동일) | `ts-complex`(2018)·`typhonjs-escomplex`(2018)·`codehawk-cli`(2023)는 모두 유지 보수가 멈추었고 최신 TS 문법 지원이 불확실합니다. 저장소가 이미 `typescript` 의존성을 갖고 있으므로 컴파일러 API로 직접 계산하는 편이 안전합니다. |
| CRAP | 자체 분석기 + Vitest 커버리지 리포트 | 함수별 사이클로매틱 복잡도와 함수별 커버리지를 결합해 계산합니다. |
| 파일당 라인 수 | 자체 분석기 (동일) | |
| 테스트 커버리지 | Vitest + `@vitest/coverage-v8` | `frontend/`에 이미 있고 `backend/`에 새로 추가합니다. |
| 생존 돌연변이 | `@stryker-mutator/core` 10 + `@stryker-mutator/vitest-runner` | |
| 죽은 코드 | `knip` 6 | 미사용 파일·export·의존성을 한 번에 다룹니다. `ts-prune`은 2021년 이후 정지 상태입니다. |
| 중복 코드 | `jscpd` 5 | |
| `any` / `unknown` | Biome `suspicious/noExplicitAny`(error) + 자체 스캔 | `as unknown as`와 `biome-ignore` 억제는 Biome 규칙으로 잡히지 않으므로 별도 스캔이 필요합니다. |

### 자체 분석기에 관하여

`tools/quality/` 아래에 TypeScript Compiler API를 사용하는 분석기 하나를 둡니다.
파일을 순회하며 함수별로 사이클로매틱 복잡도·인지 복잡도·할스테드 지표·라인 수를
계산하고, Vitest가 만든 `coverage-final.json`의 함수별 실행 정보와 결합해 CRAP까지
산출한 뒤, 위반 목록과 JSON 리포트를 함께 출력합니다.

여기서 짚어 둘 점이 하나 있습니다. **CRAP은 커버리지가 100%일 때 사이클로매틱
복잡도와 같아집니다.** 정의가 `복잡도² × (1 − 커버리지)³ + 복잡도`이므로 커버리지가
1이면 뒤쪽 항이 사라지기 때문입니다. 즉 지표 5번과 1번을 달성하면 6번은 자동으로
따라옵니다. CRAP 기준이 실제로 의미를 갖는 구간은 커버리지를 100%로 끌어올리는
과도기뿐이며, 그 기간 동안 "복잡한데 테스트되지 않은 함수"를 우선순위 순으로
드러내 주는 역할을 합니다.

---

## 5. 단계별 실행 계획

### 0단계 — 계측 (게이트 없음)

1. `tools/quality/` 분석기를 작성합니다.
2. `backend/`에 `@vitest/coverage-v8`을 추가하고 커버리지 설정을 넣습니다.
3. `knip`·`jscpd`를 추가하고 설정 파일을 작성합니다.
4. `npm run quality:report`로 11개 지표 전체의 **현재 수치**를 산출해
   `docs/quality-baseline.json`에 기록합니다.
5. 이 기준선을 보고 3절의 결정 사항을 확정합니다.

이 단계는 아직 아무것도 막지 않습니다. 실제 위반 규모를 모르는 상태에서 일정을
추정하면 반드시 틀리기 때문에, 숫자를 먼저 확보합니다.

### 1단계 — 즉시 강제 가능한 게이트

현재 상태가 목표에 가깝거나 위반 건수가 적어서 바로 `error`로 올릴 수 있는
항목들입니다.

- **파일당 500줄**: 비테스트 위반 7개(Java 제외 시 6개)를 분할한 뒤 게이트를 켭니다.
  `handler.ts`와 `server.ts`는 책임 분리가 자연스럽고, 렌더링 3종은 씬 구성·물리·
  입력으로 나눌 수 있습니다.
- **중복 코드**: `jscpd` 기준선을 확인하고 위반을 제거한 뒤 게이트를 켭니다.
- **죽은 코드**: `knip` 결과를 정리한 뒤 게이트를 켭니다. 프레임워크 진입점 등
  정당한 예외는 설정 파일에 명시합니다.
- **CI 배선**: `.github/workflows/`의 `backend.yml`·`frontend.yml`에 `quality` 잡을
  추가합니다.

### 2단계 — 복잡도 3종

- Biome의 인지 복잡도 규칙을 `error`·상한 22로 올립니다.
- 사이클로매틱 복잡도 22, 할스테드 난이도 80 게이트를 켭니다.
- 1단계에서 파일을 분할하면서 상당수는 이미 해소되어 있을 가능성이 높습니다.
  남는 것은 주로 WebSocket 메시지 디스패치와 게임 상태 전이처럼 분기가 본질적으로
  많은 함수인데, 이런 경우에는 조건 분기를 조회 테이블이나 상태 기계로 치환하는
  방식으로 접근합니다.

### 3단계 — 타입 이완 제거

- Biome `noExplicitAny`를 `error`로 올립니다.
- `as unknown as` 67건과 `biome-ignore` 14건을 하나씩 검토합니다. 상당수는 테스트
  스텁(`rtcStubs.ts`·`threeStubs.ts`)과 외부 API 응답 파싱(`kakaoClient.ts`·
  `googleClient.ts`·`roomApi.ts`)에 몰려 있습니다. 후자는 이미 의존성에 있는 `zod`로
  스키마를 정의하면 `unknown`을 경계에서 한 번만 다루고 안쪽은 타입이 확정됩니다.
- 외부 경계에서 `unknown`을 아예 쓰지 않기는 어렵습니다. `zod` 파싱 함수의 입력
  위치처럼 정당한 자리는 예외로 목록화하고, 그 목록이 늘어나지 않도록 개수를
  래칫으로 고정하는 방식을 제안합니다.

### 4단계 — 커버리지 100%

가장 긴 단계입니다. 한 번에 100%로 올리지 않고 디렉터리 단위로 진행합니다.

1. `backend/`의 현재 커버리지를 측정하고 임계값을 현재 수치로 고정합니다(래칫 시작).
2. 순수 로직 모듈(게임 규칙·점수 계산·상태 전이)부터 100%로 올립니다. 여기가
   투자 대비 효과가 가장 큽니다.
3. 인프라·배선 계층(`server.ts`·`handler.ts`·API 클라이언트)을 올립니다.
4. 렌더링 계층은 3절 (c)의 결정에 따릅니다.
5. 임계값을 올릴 때마다 되돌아가지 않도록 CI에서 고정합니다.

### 5단계 — 생존 돌연변이 0

- Stryker를 `backend/`의 순수 로직 모듈에만 먼저 적용합니다. 전체 코드베이스를
  대상으로 삼으면 한 번 실행에 수 시간이 걸려 CI에서 쓸 수 없습니다.
- CI에서는 매 PR이 아니라 야간 스케줄로 실행하고, PR에서는 변경된 파일만
  대상으로 하는 증분 실행을 사용합니다.
- 대상 범위를 모듈 단위로 넓혀 가며 각 범위에서 생존 돌연변이 0을 달성합니다.

### 6단계 — `interlinked-cli` 도입(선택)

앞 단계에서 만들어 둔 리포트를 소비하도록 연결합니다.

- `interlinked enable`로 훅과 스킬을 설치합니다.
- `interlinked coverage`와 `interlinked mutation`이 각각 Vitest와 Stryker의
  리포트를 읽도록 설정합니다.
- `strict_typing_block`을 `.interlinked/guard-rules.json`에서 활성화합니다.
- 모드는 `balanced`로 시작해 안정화된 뒤 `strict`로 올립니다.

---

## 6. 규모에 관한 솔직한 평가

비테스트 TypeScript 코드가 약 5만 2천 줄입니다. 여기에 100% 커버리지와 생존
돌연변이 0을 적용하는 작업은 몇 주가 아니라 몇 달 단위의 일입니다. 특히 5단계는
난도가 4단계보다 훨씬 높습니다. 커버리지 100%는 "모든 줄이 한 번은 실행됨"을
뜻할 뿐이지만, 생존 돌연변이 0은 "모든 줄의 모든 변형이 어떤 테스트든 실패시킴"을
요구하기 때문입니다.

그래서 다음과 같이 접근하기를 권합니다.

- 1~3단계(라인 수·중복·죽은 코드·복잡도·타입)는 **전 범위에 절대 기준으로**
  적용합니다. 위반 건수가 적고 효과가 즉시 나타납니다.
- 4~5단계(커버리지·돌연변이)는 **순수 로직 핵심에는 절대 기준으로**, 나머지에는
  **하락을 막는 래칫으로** 적용합니다.
- 예외는 반드시 설정 파일에 파일 단위로 명시하고, 예외 목록의 길이 자체를 CI에서
  감시해 늘어나지 못하게 합니다. 이렇게 하면 기준을 낮추지 않으면서도 진행이
  멈추지 않습니다.
